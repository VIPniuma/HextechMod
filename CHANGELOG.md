# Changelog

## 0.3.49（2026-09-16）

### 修复：技能 HUD 代币条两行文字重叠

- 「◈ N 代币」与「按 L 打开商店」这两行原来在钱包条里挤在一起、几乎叠到一块。
- 钱包条加高（76 → 84px），代币行上移、商店提示行下放，中间留出间距。

### 修复：打开游戏（主菜单）就检查版本更新，不再等进到机场

- 版本检测本来就有，但原来卡在「场景名 == Airport」才查；一旦 PEAK 改了主菜单场景名或加载时序变化，检测就整段失灵（打开游戏收不到提示）。
- 现在改成**插件一启动就查**（启动后缓 2 秒等主菜单入场动画落定，直接拉 version.json），不再依赖任何场景名 —— 打开游戏、还在主菜单就有新版本会立刻弹提示。

## 0.3.48（2026-09-16）

### 新增：F9「游戏玩法」分类 + 商店总开关

- 设置面板（F9）新增「游戏玩法」分组：把 `启用商店` / `共享代币` / `每分钟代币` / `物价倍率` / `功能溢价` 从「商店」归到「游戏玩法」一组；`打开商店` 按键仍留在「商店」。
- 新增 `启用商店` 总开关（默认开，记入更新保留列表，关掉后不被版本默认值覆盖）：
  - 关店后**代币整体停止积累**（`AccrueTokens` / `AddTokens` / 共享池三处都加 `!ShopEnabled` 闸门，被动 / 拾荒者 / 行李箱 / 中途加入 / 共享池全部停）；
  - 按 `L` 打不开商店（开着则立即收起），技能 HUD 钱包条关店时隐藏；
  - **关店联动调价**：`ShopPricing.CanEdit` 追加 `&& ShopEnabled`，店内 `T` 调价与 F9 单件调价一并置灰，「每分钟代币」「物价倍率」「功能溢价」三个倍率项关店时也置灰并拦截点击（提示「商店已关闭，无法调价」）。

### 新增：共享代币（联机共享池）

- 新增「共享代币」开关（仅机场可设、房主权威共享池、联机同步、商店扣费走共享池）：开启后全房间共用一个代币池，结算与显示都走 `SharedTokenPool`。

### 调整：拾荒者改为小数收入

- 每件物品 +0.3 枚（`ScavengerTokenPerPickup = 0.3f`，小数累计），保留「不设上限」；HUD 用 `FormatTokens` 向下取一位小数显示。原来「每件 +1 枚」在 PEAK 里等于白嫖几十枚、商店变免费，太超标。

### 调整：鬼魂 / 死亡 / 昏迷不再累计代币

- `AccrueTokens` 顶部加守卫：处于鬼魂、死亡、昏迷状态时不再积累代币。

### 重做：挂机惩罚改为「篝火附近不积累代币」

- 删除原来的「篝火刷蘑菇僵尸」机制（`CampfireZombieGuard`），改为**在篝火 25 米范围内不积累代币**（配置 `CampfireAfkGuard`）：净站着不动只停发代币，不再招怪；走出范围照常继续。

### 修复：死亡重抽复活后奖励不丢

- 行李箱 / 篝火奖励的领取记录原来挂在角色身上，复活重建角色会丢；`TryClaimCampfireReward` 改为按稳定 actor 号存静态字典 `ClaimedRewardsByActor`，复活后仍能认领、回机场清空。

### 调整：商店下架两件

- 「潘多拉魔盒」「诅咒之书」不再上架（`HextechShopCatalog.AddItem` 用中文显示名匹配拦截，prefab 名兜底），其余商品照常。

## 0.3.32（2026-09-14）

### 调整：第三轮词条修正（2026-09-14）

- **新增：拿取上限与替换** —— 普通海克斯最多同时持有 **4 条**、技能解锁词条最多 **1 条**
  （同词条叠层不占名额）。满了之后在三选一 / 商店 / 行李箱拿到新词条时，会弹出替换面板：
  点一条旧的把它**连效果一起移除**（摘掉它挂的状态、数值还原到开局基准后重放剩余词条），
  新词条顶上；点面板最后一张「新词条」则放弃。ESC 关掉面板会重新弹出，替换是必选的。
  上限数值（普通 4 / 技能 1）可在服务器配置里调整。
- **新增词条「精致胃袋」（白银）**：吃野生食物（浆果、蘑菇等）效果减半；吃包装食物
  （棉花糖、烤肠、坚果等袋装食品）效果翻倍；运动饮料、能量饮料和棒棒糖不受影响。
  拦在 `Action_ModifyStatus.RunAction`（食物对状态条的所有加减），改完放行 —— 联机同步 / 喂食统计照常；
  buff 类效果（护盾加速等）不缩放；烤过的野生食物仍按野生算。不可叠加。
- **全部主动技能都有使用代价了**（不许无代价白嫖）：「漂浮」使用后立刻 **+10 饥饿**、
  「肾上腺素」使用后立刻**透支 15 饥饿**、「无敌」使用时立刻 **+5 石化**。
  疾风步 / 净化（饥饿）、治疗波 / 机械手（石化）本来就有，维持不变。
  所有代价都走游戏原生的 `AddStatus` 路径：无敌期间被原版逻辑拦下（无敌自己的代价**先付后开盾**，
  否则会被无敌免疫掉）、篝火「不饿」buff 照常生效、石化类代价对「石化抗性」持有者按 1/3 削减。
- **围炉谈心**：改成「与队友挨在一起时，每 10 秒恢复 1 点受伤值」（原来是回体力）。
- **自我修复**：每 10 秒 → 每 15 秒回 1 点伤势。
- **治疗波重做**：从「恢复最多 55 点伤势 / 中毒 / 寒冷等负面积累（45 能量 · 20 秒冷却）」改成
  **只恢复 15 点受伤值**（不治疗其他负面状态）· **120 秒冷却** · **代价 +5 点石化值**（能量 45 不变）。
  石化代价与机械手同一条结算路径：持有「石化抗性」时实付约 +4，「无敌」期间会被原版逻辑拦下。
- **挣脱**：删除 —— 蛛网 / 捕蝇草的挣脱实际不走这条状态削减路径，词条在游戏里等于白板。
- **羽落**：从「几乎完全免疫」（-98%）改成「受到的摔落伤害 -50%」。
- **不死鸟**：从「每局 1 次、留 1 点伤势」改成**不限次数**、最多留 20 点伤势。
- **骨质疏松**：每局全队最多 1 人持有（原 2 人）；本身依旧不可叠层。
- **蜘蛛侠修复**：挂在攀爬物上不动时体力本来该冻结，实际却会缓缓**回血** ——
  归零消耗让游戏「多久没用体力」的计时一直涨，自然回体力趁虚而入；现在把计时按住，挂着不掉也不涨。
- **技能词条补介绍**：漂浮 / 疾风步 / 净化 / 治疗波 / 肾上腺素 / 无敌 / 机械手 ——
  三选一、商店、HUD 上的说明现在带技能介绍、效果与能量 / 冷却，不再只有一句「解锁主动技能」。
- **修复：僵直 / 睡不醒跨局越叠越狠** —— 回机场还原本局成长时漏还原「转向速度」与「困倦消退」两个字段，
  残缺值会被下一局当成新基准，每局再乘一次（-25% / -30% 无限叠下去）。
- **修复：不死鸟的 20 点安全线会被玻璃体质顶破** —— 兜底夹紧原来跑在「玻璃体质放大」之前，
  兜完底的量会被再放大一遍；现在系数缩放先结算、不死鸟最后出手。

### 调整：全部海克斯整体削弱一档

- 品质数值下调：青铜 10% → 8%、白银 15% → 12%、黄金 20% → 16%、传说 25% → 20%（约 -20%）。
- 所有走品质表的词条（含「领头羊」的光环与「抬轿人」）与三选一 / HUD / 商店卡片上显示的数字同步变化，
  不存在「说明没跟上」的问题 —— 数字是运行时按品质算出来的。

### 调整：第二轮超标词条削弱（按用户逐条拍板）

- **二段跳**：改**不可叠加**（原可叠 3 层 = 3 次额外空中跳，位移超标）；「投掷专家」每层 1.25 → 1.2（满层 ×1.56 → ×1.44）；
  「领头羊」上限 3 → 2；「能量饮料」上限 3 → 2。「羽落」按拍板保留 -98% 不动。
- **皮糙肉厚 / 抗毒体质**：每层 0.7 → 0.75（3 层单状态积累 -65.7% → -57.8%）。
- **收藏家**：上限 2 → 1（代币加成 ×2.0 → ×1.5）。
- **人人为我**：每层 100 → 60 额外体力（满层 200 → 120）。
- **送葬人**：30 体力 + 6 秒加速 → 20 体力 + 4 秒加速。**没有也没有再加冷却**。
- **围炉谈心**：每 5 秒回 10% 体力 → 每 8 秒回 6%（每秒 2% → 0.75%）。
- **一次性 buff 微削**：甜食冲刺 30 → 24 秒、守护者护盾 25 → 20 秒、全能治疗 80 → 64、回光返照 15 → 12 秒、士气大提升 20 → 16 秒。
- **铁律落地：词条没有冷却** —— 除主动技能（机械手等）外，任何海克斯词条一律没有 CD；
  顺手把「拉拉手」原来的 6 秒回复冷却删了（每次成功拉手都回体力）。
  送葬人 / 人人为我补丁里的 2 秒窗口是「同一次死亡 / 倒地走两条 RPC」的去重（挡重复结算，不是 CD），保留。

### 重做：石化抗性不再「完全免疫石化」

- 原效果：外界的石化值一点都攒不上来，代价只是石化值自己每分钟 +1% ——
  等于所有「副作用是加石化值」的东西全部白给（机械手的 +5 代价、爬石化石壁、按秒加石化的护符），太超标。
- 新效果：**受到的石化值积累 -1/3** —— 本该 +30 只会 +20，石化压力还在，只是来得更慢。
- 削减点收口在 `AddPetrify(int)` 一处（状态条路内部最后也走它），不会双重打折；
  小数「省下的点数」攒整再拦，长期期望正好 -1/3（机械手 +5 → +4，每秒 +1 的持续来源约每三次拦一点）。

### 修正：叠层规则统一（最多 3 层 + 乘法叠加）

- 「轻装疾行」上限 4 → 3、「耐力储备」上限 5 → 3、「机能零食」上限 5 → 3 —— 全局不再有超过 3 层的词条。
- 「机能零食」的叠层从「每层线性再叠一份 5%」（三层 15%）改成与品质词条同一套乘法叠加：
  5% → 5.25% → 5.51%。
- 「围炉谈心」从「每 5 秒回 10% 体力」（每秒 2% 上限）削到「每 8 秒回 6%」（约每秒 0.75%）
  —— 原数值在青铜档里一骑绝尘。固定值词条（自我修复 / 温暖之躯 / 饱腹 / 吐纳 / 挣脱 / 清醒）叠层保持线性不变。

### 修复：物品直接进背包时拾取类海克斯失效

- 日志体检发现 `Item.PutInBackpackRPC` 现在只有 `slotID` / `backpackReference` 两个参数，而 `BackpackPickup` 补丁仍按三参数注入，Harmony 报 `No parameter found at index 2` 后整组补丁跳过。
- 已改为按当前签名的 `__0` / `__1` 注入，让「拾荒者」等依赖背包直入路径的效果恢复结算。

### 修复：按 `ESC` 收起三选一、再按 `Y` 重开就能无限刷新

- 玩家反馈：刷新（换牌）完之后按 `ESC` 退出，再按 `Y` 重开，刷新次数又回来了 —— 而且重开时连牌都是重新抽的一副。
- 原因：三选一的牌是**每次弹面板时现抽**的 —— `HextechPanel.Show()` 每次都重抽一副、`RebuildCards()`
  重建卡片（挂在卡片上的 `RerollUsed` 跟着归零），`Close()` 还会把 `_entries` 清掉。
  于是「玩家按 `ESC` 收起来、机会留着」这条设计被走成了「`ESC` + 重开键 = 免费重抽 + 刷新次数无限」。
- 修法：给每次机会一个编号（`HextechManager.PendingChoice.Key`，`QueueChoice` 里自增，永不为 0 / 负数），
  面板记住自己摆的是哪一次（`HextechPanel._offerKey`）：
  - 编号没变 → **原样摆回**上次那几张（含哪张刷过、刷成了什么），连抽牌函数都不会被调用；
  - 编号变了（新的一次机会）才重新抽牌、重建卡片；
  - 「怎么抽」从调用方抽好再传进来改成**传一个抽牌函数**（`Show(offerKey, rollOptions, …)`），由面板决定要不要抽；
  - `Hide()`（玩家真的选了）才作废这次摆过的牌；`Dismiss()`（`ESC` / 窗口被游戏关掉）保持原样。
- 顺带：面板注释与 README 的口径从「每次弹出每张限刷一次」改成「**每次机会**每张限刷一次」。

### 修复：上传日志「按了键一直停在正在上传、半天没有编号」

- 现象：按下 `F10` 后屏幕上一片安静，再按一次只回一句「日志还在上传中」，最后既没有编号也没有失败提示。
- 查证：① 服务器 `/www/hextech-reports` **一份报告都没有** → 请求根本没到；② 本机 `LogOutput.log` 只留下
  「`[上报] 开始上传，压缩后 16 KB。`」，既无成功也无失败 → 卡在发请求那一步；③ 同一台机器手工 POST 一次
  （同样的头、同样的 gzip 体）**190 ms 就回了编号** → 服务端没问题；④ 那次压缩后只有 16 KB →
  **不是日志太大**，是 Unity 那条网络栈（`UnityWebRequest` / libcurl）没回来。
- 修法（`Core/LogReporter.cs`）：
  1. **多一条通道**：先走 Mono 的 `HttpWebRequest`（后台线程发、`Proxy = null` 关掉系统代理自动探测 ——
     那是「请求干等到超时」的经典成因），不成再换 Unity 的 `UnityWebRequest`，两条完全不同的栈；
  2. **看门狗**：每条通道最多 20 秒（另加 5 秒宽限），`UnityWebRequest` 那条到点直接 `Abort()` ——
     不管底层怎么卡，玩家一定会拿到一句话，而不是永远停在「正在上传」；
  3. **失败也有产出**：两条都不通时把报告落成 `BepInEx\HextechMod-report.txt`，玩家直接把文件发给作者即可；
  4. `Busy` 带过期（150 秒）：万一协程被意外掐断，`F10` 不会永远只回「还在上传中」。
- 顺带两件事：
  - **每步都有回声**：屏幕上方依次报「正在打包日志（只取最近 10 分钟）…」→「正在上传日志（N KB）…」→
    每秒一次的「已等 N 秒」，不再有分不清「在传还是卡死」的空白；
  - 上报键**不再依赖本机角色**（调用点移到角色判空之前）：卡在主菜单 / 加载界面、根本没进得去游戏时也能传。

### 优化：日志只取「按 F10 那一刻往前 10 分钟」

- 起因：玩家报 bug 时 `Player.log` 可能已经几十 MB，整份传又慢又没必要 —— 要查的问题基本都在按键前那几分钟里。
- 两份日志**都没有时间戳**（BepInEx 5 的 `LogOutput.log` 只有 `[级别 : 来源] 内容`，Unity 的 `Player.log` 只有裸内容），
  没法按行筛时间，所以换一种量法：`LogReporter.Tick()` 每 30 秒（`HextechManager.Update` 里调）记一次
  「现在几点 + 两份文件各多少字节」，上传时用样本里最接近「十分钟前」的那条反推起始字节，从那里读到文件尾。
  这次会话还不满十分钟（游戏刚开就出问题）时自然就是整份 —— 报告里会写明实际取了哪一段。
- 各份另有上限兜底（BepInEx 256 KB / `Player.log` 512 KB），遇上刷屏日志也不会传上去几 MB；
  报告开头新增一行「日志取样 : 最近 10 分钟（靠 N 个位置样本反推起点）」。
- 配置项说明与 README 同步改成「最近 10 分钟」的口径（原来写的是「尾部 512 KB / 1 MB」）。

### 新增：上传日志后弹「取件编号」浮窗，设置面板里也能翻到本机上传记录

- 起因：编号原来只在屏幕顶上停 10 秒 —— 玩家一眼没看见、或者剪贴板随后被别的东西顶掉，就再也找不着了，
  作者只能让他重传一整份；而「编号是多少」恰恰是反馈 bug 时最常卡住的一步。
- 上传成功后弹一个浮窗（`UI/HextechReportCodeHud.cs`）：**大号 6 位编号** + 「复制编号」 + 「确定」，
  **停在屏幕上等玩家自己点「确定」才关**（不自动消失）；弹出时先自动写一次剪贴板，被顶掉了可以再点「复制编号」（点完变「已复制」）。
  浮窗走游戏的窗口系统（`HextechWindowHost`）来解锁鼠标 —— 这个模组的界面一律手算鼠标位置、没接 EventSystem。
- 「海克斯设置」（`F9`）列表最下面新增一节「日志上报 · 本机上传记录」（`Core/ReportHistory.cs`）：
  按时间倒序列出最近 6 份（编号 · 上传时间 · 压缩后大小 · 当时场景 · **还剩几小时**），点一行就把编号复制回剪贴板。
  记录存在 `.cfg` 同目录的 `HextechMod-reports.txt`（最多留 12 条），**和服务端一样 48 小时后自动过期**。
- 顺带：关掉模组开关时把浮窗一起收掉；浮窗开着时输入归它（三选一 / 商店 / 设置面板都不抢），
  否则点「确定」的那一下会同时穿到下面的配置行上。

### 修复：防挂机会在机场那边误判「站在篝火边」（刚上机 / 刚落地就被招僵尸）

- 玩家反馈：在机场那片被招蘑菇僵尸，而旁边根本没有篝火 —— 和他要求的「只有篝火附近才算」不符。
- 原因：判定只比「离任意**点着的**篝火 ≤ 半径」，**不分场景**。机场那张场景在跑图时可能还挂着，它里面的篝火
  （常设 `forceLit` 的装饰火）坐标正好压在上机 / 落地那一片，于是站在地图上根本没有火苗的地方也会被算成「在篝火边」。
- 修法（`Core/CampfireZombieGuard.cs`）三条同时卡死：① 角色自己就在机场场景里 → 直接不算
  （用**角色所在场景**判断，不再只看「活动场景」）；② 篝火必须和玩家在**同一张 Unity 场景**里；
  ③ 篝火必须是 `state == FireState.Lit`（真在烧），不再接受 `Lit` 里那个 `forceLit` 分支。
- 顺带：召唤时往 BepInEx 日志写一条（谁 / 哪只篝火 / 哪张场景 / 相隔多少米），以后再有类似反馈直接看日志就有答案。

### 新增：遇到 bug 一键上报日志（按 `F10` → 回一个 6 位数字取件编号）

- 起源：玩家反馈 bug 全靠手打，作者看不到日志，只能来回问。
- 客户端（`Core/LogReporter.cs` + 配置「界面 / 上传日志」，默认 `F10`）：把「本机状态快照 + `.cfg` 原文 + BepInEx 日志尾部 512KB + 游戏日志尾部 1MB」
  拼成一段纯文本、自己 gzip、以 `octet-stream` POST 到更新源同目录的 `report.php`；成功后把 6 位数字编号写进剪贴板、在屏幕上停留 10 秒。
  地址仍由构建时注入的更新源推导（`version.json` → `report.php`），源码里不出现服务器地址。**只有玩家按键才会传，没有自动上传。**
- 服务端（`server/report.php` + `server/nginx-hextech.conf`，已部署并自测通过）：不解压、原样存盘，回编号；
  报告落在**站点根目录之外**的 `/www/hextech-reports/<编号>/`（URL 拉不到，只有 SSH 进得去），同一 IP 每天 20 份，超过 60 天自动清理；
  站点只放行这一个脚本，不启用全站 `.php` 解析。
- 作者侧取件：`_tools/fetch-report.ps1 -Code 482915`（拉回 `artifacts/reports/<编号>/` 并解压、默认打报告开头），`-List` 列出最近 30 份。
- 编号是**纯 6 位数字**（首位不给 0）—— 玩家是隔着聊天窗口手打过来的，字母容易看错；
  另一个坑：`fetch-report.ps1` 里远端多行命令要先 `-replace CRLF→LF`，否则 bash 会对 `done` 报语法错。
- 报告内容随后又扩了一轮（`Core/LogReporter.cs`），因为它是**整个模组**的取证通道、不该只看得见本机：
  ① **全房间角色** —— 房间里每个角色的场景 / 坐标 / 死亡·昏迷·僵尸·幽灵状态 / 词条与层数 / 代币，原来只有本机视角，
     而「队友拿到某条词条之后全场开始掉帧」「房主侧判定和客户端对不上」这类问题一半在别人身上；
  ② **全局机制状态** —— 防挂机开关 + 房主侧每人累计秒数（`CampfireZombieGuard.Describe()`）、本机与全房间禁用名单、
     商店改价件数、本局阶段与已打时长；
  ③ **补丁体检** —— `Plugin.PatchAllSafe` 现在统计成功 / 失败的补丁类，失败清单连同原因一起上报，
     插件启动时那行日志也会写「补丁 N 组打上，另有 M 组失败」。

### 变更：上报的报告只留 48 小时（服务器每天 00:00 自动清理）

- 原来的口径是「保留 60 天，靠上传时 1/20 的概率顺手清一次」—— 存得久、但清理完全看运气，
  而且这是一台只有 40G 盘的机器，报告一份几 MB，攒着没意义。
- 现在：服务器每天 **00:00** 跑一次 `php /www/wwwroot/hextech/report.php`（`server/report.php` 新增的 CLI 分支，
  只做清理、不碰 HTTP 逻辑），删掉**超过 48 小时**的报告目录，顺带清掉 7 天前的限流计数文件
  （`.ratelimit/*.txt` 按天一个文件，原来一直没人清）。
- 上传接口那条路照旧：另外保留一条「距上次清理超过一天就趁有人上传顺手清一次」的兜底，怕定时任务被面板冲掉。
- ⚠ 取件窗口只有 48 小时：玩家发来编号要尽快 `_tools/fetch-report.ps1 -Code …`，隔两天再看就没了
  （`fetch-report.ps1` 的报错提示已同步说明这一点）。
- 已在服务器上实测：3 天前的假报告被删、刚建的保留、10 天前的限流文件被删；定时任务装在 `www` 的 crontab 里
  （和 php-fpm 同一个身份，清理产生的文件属主一致），root 的 crontab 没动。

### 新增：报告里多一节「词条触发统计」（哪条词条本局到底跑没跑）

- 起因：玩家说「某条词条好像没生效 / 数值不对」时，原来的报告只能看到「他有哪些词条、几层」，
  看不出效果有没有真的落地，只能让对方口述，来回好几轮。
- 做法：`Core/HextechState.cs` 给每条词条挂上三本账：
  ① **触发次数** —— 效果真的落地了几次（`RecordEffect`，埋点写在效果生效的那一行）；
  ② **累计影响** —— 「这条词条一共影响了多少」：削减类记削减量（羽落减掉的伤势、坚韧之躯压掉的状态积累、
     石化抗性拦下的石化值…），增益类记增益量（吐纳回的体力、拾荒者发的代币…）；
     单位随词条（代币 / 状态点 / 秒 / 力度…），只跟同一条词条纵向比，别跨词条相加；
  ③ **被动在场** —— `Tick` 里按帧累加「在场多少秒 / 多少帧」，覆盖所有常驻词条。
     数值型词条在获得那一刻就把数值乘上去了，本身没有可计数的「触发」，这部分只能靠在场时长回答「它有没有在跑」。
- 报告新增「词条触发统计（本机 · 本局）」一节，分「触发 / 0 次 / 被动」三组；带 `※` 的是**埋了点**的词条，
  显示「0 次」就等于「本局一次都没生效」，那才是要查的；没埋点的只报在场时长。
- 只有本机角色有统计 —— 效果是在各自的机器上结算的（别人的效果在对方机器上跑）。
- ⚠ 以后每加一处埋点，就往 `LogReporter.EventCountedIds` 加一条 id，
  否则报告会把「没埋点」和「没触发」混在一起，反而误导排查。

### 修复：「背了昏迷的队友，他醒了之后放不下来」（给原版的放手逻辑打兜底）

- 现象：队友完全昏迷时把他扛起来，队友中途醒了，人就一直挂在肩上 —— 按交互键没反应、也不出 `DROP` 提示，怎么都放不下来。
- 原因全在原版（与本模组的词条无关）：
  - 能扛的只有「完全昏迷」的队友（`CharacterInteractible.CanBeCarried` 要求 `fullyPassedOut && !dead && carrier == null`），
    而**醒来时原版什么都不做** —— `Character.UnPassOutDone` 只清 `fullyPassedOut` / `passedOut`，完全不碰扛人关系；
    `CharacterCarrying.Update` 也只会在**扛人者自己死亡**时自动放下。全游戏只有 `RPCA_StartCarry` / `RPCA_Drop` 会写 `carrier`。
  - 手动放下的唯一入口是 `Interaction.DoInteractableRaycasts` 里那条特殊分支（扛着人**且**当前选中槽正好是背上那个槽，才把肩上的人直接当目标）；
    其余情况靠射线扫描，而扛起时 `CharacterRagdoll.ToggleCollision(false)` 已经把被扛者的碰撞体全部关掉 —— 扫不到人，交互提示自然也不出。
- 修法：新增 `Core/CarryGuard.cs`（配置项「修复 / 扛人兜底」，默认开）。本地玩家肩上的人一旦不再完全昏迷（醒了）或已经死亡，
  就用原版自己的 `Character.BreakCharacterCarrying(true)` 广播放下，走的是和手动放下完全相同的那条 RPC。
- 边界：放下这条 RPC 必须由扛人者的客户端发出（`RPCA_Drop` 打在扛人者的 PhotonView 上），所以联机时「扛人的那位玩家」也要装这个模组才生效；
  同一个目标 3 秒内只尝试一次（RPC 是异步的，避免刷屏），目标下肩后计数自动清零。

### 修复：「蜘蛛侠」挂在边缘（抓住墙沿 / 钉子把手）时照样掉体力

- 现象：拿到「蜘蛛侠」之后爬墙挂着不动确实不掉了，但**挂在边缘**吊着还是照掉。
- 原因有两层：
  1. 原实现盯的是 `CharacterData.staticClimbCost`，而这个字段根本不是「静止不消耗」的开关 ——
     `CharacterClimbing.Update` 里它只决定「要不要再乘角度修正 / 地形修正」（`brtrue` 直接跳过那几项乘法），
     静止时的消耗是被夹到 `minStaminaUsage`，从来不是 0。所以它停不下体力。
  2. 就算这个字段有用，纠偏条件写的是 `isClimbingAnything`（= 墙上 / 绳梯 / 藤蔓），
     而「抓住墙沿 / 把手吊着」属于 `currentClimbHandle != null`、**不在** `isClimbingAnything` 里 ——
     挂边缘时那段纠偏压根不会跑。
- 修法：改到**消费端**收口。三条攀爬路径最后都调 `Character.UseStamina`：墙面与挂把手在
  `CharacterClimbing.Update`（静止时被夹到 `minStaminaUsage`）、绳梯在 `CharacterRopeHandling.Update`、
  藤蔓在 `CharacterVineClimbing.Update`（静止时是写死的 0.005/秒）。现在在 `UseStamina` 前缀里统一判断
  「挂在攀爬物上（`isClimbingAnything` 或 `currentClimbHandle`）+ 方向输入小于 0.05」就把这一笔消耗归零，
  松开摇杆挂在墙上 / 绳梯上 / 藤蔓上都是一点不掉，往上爬照旧按输入幅度扣。
- 两个细节：
  - **攀爬跳不算静止**：`RPCA_ClimbJump` 里那次固定消耗用 `jumpWasPressed` 排除掉，
    所以这条词条给的是「能安心挂着歇」而不是「攀爬免费」。
  - `UseStamina` 第一句是 `if (usage == 0) return false;`，而绳梯 / 藤蔓把「返回 false」当成
    **体力见底**处理 —— 直接把人从绳子 / 藤蔓上踢下去。所以归零之后要把返回值扶成 `true`
    （只改我们动过的那一次），人还挂得住。

### 「空灵之体」的永久低重力不再循环播放旋风风声

- `Affliction_LowGravity.OnApplied` 会调 `CharacterAfflictions.StartWhirlwind`，而它最终执行的是
  `SetWhirlwindStageRPC(Start)` 里那次 `whirlwindSFX.Play()` —— 一段**循环**的低重力风声。
  原版只有临时性浮空（道具几十秒）所以不刺耳，「空灵之体」是永久低重力，这段声音就变成整局的背景噪声。
- 现在挂上永久低重力时把角色的 `whirlwindSFX` `mute` 掉（**只关音效、保留脚下那圈旋风粒子** ——
  那是低重力唯一的视觉提示）；回机场清空本局成长时（`RemoveAppliedAfflictions`）恢复，
  此时身上若还有别的浮空道具，它的风声照常响。只作用于本机的 AudioSource，不影响别人听到的声音。

### 修复：无敌状态叠层时会变短（15 秒的「回光返照」被 6 秒的技能「无敌」顶掉）

- 原版 `Affliction_Invincibility.Stack` 是 `totalTime = other.totalTime` + `timeElapsed = 0` ——
  后获得的那个无敌时长会**无条件**顶掉当前的。所以「回光返照」那 15 秒里用一次 6 秒的技能「无敌」
  （或捡到更短的无敌道具），剩余时间会直接缩成 6 秒。
- 现在叠层改成取「两者中更长的**剩余**时间」：既不缩短，也不会凭空延长。
  「第一次获得无敌」（没有同类状态、不走叠层）的时长不受影响。

### 修复：「羽落」只护得住第一次摔落（骷髅身上还更严重）

- 玩家反馈「拿到羽落、用骷髅书变成骷髅之后，轻轻摔一下就死」，根因有两层：
  1. 「羽落」原来是用原版 `CharacterMovement.CapFallDamage(0.02f, float.MaxValue)` 实现的，
     而那个 API 是**一次性窗口**：窗口内第一次结算就把 `fallDamageCapEndTime` 设成「现在」，
     之后再也不生效（原版只有 `AOE.Explode` 拿它兜爆炸击飞那一下）。所以这条词条实际只免掉**一次**摔伤，
     第二次起完全没有减免 —— 玩家以为一直挂着，其实早就是裸的。
  2. 骷髅在原版里受到的**伤势被硬编码 ×8**（`CharacterAfflictions.AddStatus` 里
     `isSkeleton && Injury → amount *= 8f`）。这跟「免疫绝大部分负面积累」是两回事，骷髅就是更脆。
- 现在「羽落」改成在 `AddStatus` 前缀里按层数乘 `FeatherFallFactorPerStack`（0.02 = 摔伤只剩 2%），
  **每次摔落**都生效，代价（地面移动速度 -8%）不变。
- 骷髅**不比人形特殊**：羽落是**比例**减免，系数乘在原版那层骷髅 ×8 **之前** —— 同样一摔，人形剩 0.02 份、
  骷髅剩 0.16 份，骷髅依旧比人形疼 8 倍（「骨质疏松」写明的代价，不在这里替它免掉）。
  骷髅身上**不要**再把 ×8 除回来（那等于把这条原版代价一并抹掉）。
- 「不死鸟」同一处的 ×8 处理同理：它的「距倒地线还差 1 点」是**绝对量**，不先折回 1/8 会被放大 8 倍直接顶满，
  兜底形同虚设。
- 「骨质疏松」说明补上这条原版代价：**受到的伤势 ×8**（原有的「死亡不留骸骨、无法复活」照旧）。
- 顺带修掉一处静默失效：回机场清本局成长用的是 `RemoveAffliction(我们手里的实例)`，
  但 `AddAffliction` 遇到同类会把内容 `Stack` 进**表里那条**（我们的实例根本不进表），
  而 `RemoveAffliction(实例)` 走的是 `List.Remove` 的引用比较 —— 「表里已有同类」时那条根本删不掉。
  已改成按**类型**移除（`RemoveAffliction(AfflictionType)`）。

### 商店代币改成「连着涨」，并且显示到一位小数

- 代币原来是「每满一个间隔 +1」的整数计数器，速率不是整数（默认 1.5/分 = 每 40 秒 1 枚）时那半枚根本看不见。
  现在改成**按帧连续累积**：1.5/分 就是每秒 +0.025，`HUD` 钱包条、收起后的小提示、商店右上角的徽章
  统一显示到**一位小数**（`◈ 3.4 代币`），一眼能看出它在涨；攒够新的一整枚时仍然弹一次「商店代币 +1」。
- 显示**向下取整**：3.97 显示 `3.9` 而不是 `4.0`，免得出现「显示 4.0 却买不起 4 枚的东西」。
  消费照旧按整数价格扣，扣完剩下的零头（买 4 枚剩 0.4）留在账上，不会被抹掉。
- 「中途加入补足代币」也换成同一口径：以前按「错过的间隔数」取整补，速率为 1.5/分 时每段零头都被抹掉；
  现在错过多久补多少（例：错过 100 秒 = 补 2.5 枚），提示里也带小数。
- 「收藏家」（速率 +50% / 层）与「资本家」（+0.5 枚/分 · 层）的加成，以前只能体现在「下一个间隔变短」，
  现在直接体现在数字往上走得快慢上。

### 价格预设：调好的价可以存成多份、随时换回来

- 商店调价模式（按 `T`）右下角、`F9` 面板「商店调价」那一段里新增两颗按钮：
  **「保存为预设」**（把当前这版价格存起来，自己起名字，最多 12 字，同名整份覆盖）与
  **「选择预设 ▾」**（列出「默认配置」+ 所有存过的预设，点一份整张表就换过去；每份可以删，删除要点两下确认）。
- 一份预设 = **逐件调价表** + 三个价格数值（`商店.物价倍率` / `商店.功能溢价` / `商店.每分钟代币`）。
  把它们打包在一起是因为实际售价由这几项共同决定，只存逐件价的话换预设还得回去手动改倍率。
  预设存在 `BepInEx/config/HextechMod.prices.presets.txt`（每个预设一段，段内键值用制表符分隔），
  和逐件调价表放同一个目录、不进 `.cfg`。
- **更新不再让老玩家白调一遍**：装上本版后第一次启动时，先把玩家更新前的价格设置备份成预设
  「自定义价格表1」，然后再执行原有的「配置版本对不上就把数值刷回本版默认值」。备份只做一次
  （记忆写在预设文件里），之后每次更新都不再重复；玩家随时能在选择框里点回那一份。
  全新玩家（没调过价、数值也全默认）不会凭空多出一份预设。
- 弹层是自建的：列表、按钮、名字输入全部手算鼠标位置、文字直接读 `Input.inputString` ——
  模组界面不接 EventSystem，UGUI 的 `Dropdown` / `TMP_InputField` 在这里根本跑不起来。
  弹层打开期间下面的界面整帧跳过输入，所以同一次点击不会穿透到后面的卡片和按钮上。
  （游戏里输不进中文，想要中文名可以去 config 目录手改预设文件。）
- 已知限制：名字用游戏内输入法只能打英文数字；预设只覆盖上面说的那些项，开箱概率、词条权重等不受影响。
- 修复：点「选择预设 ▾」可能**没反应**（弹层一闪就被关掉）。原因是打开弹层的那一次鼠标点击同时被弹层自己
  看到了 —— 弹层的 `Update` 比宿主界面晚跑，于是那一下「按在屏幕底部、既不在列表行上、也不在卡片里」的点击
  命中了「点卡片外面 = 取消」分支。现在打开弹层的那一帧不收输入（与已有的「刚关掉那一帧也屏蔽」正好对称）。
  顺带把列表命中判定限制在滚动视口内（`RectMask2D` 只裁渲染、不裁命中），点标题区不会误选到滚出视口的行。

### 防挂机：在篝火边站桩太久，会被蘑菇僵尸找上门

- 在**点着的**篝火 25 米范围内连续待满 **3 分钟**（默认），就在这名玩家附近召唤一只原版蘑菇僵尸并**锁定**他。
  走出范围**不会**立刻清零，而是按同样的速度往回退（退到 0 为止）—— 打的是「净站着不动」而不是「在篝火附近出现过」，
  短暂出去拿个东西再回来是接得上的；召唤过一次之后计时也归零，
  继续站着就每 3 分钟再来一只。
- 判定放在**房主**侧：只有房主能 `PhotonNetwork.Instantiate` 网络物体，而且这样不依赖挂机那个人的客户端
  还在正常工作。单机（自己就是房主）同样生效；房主没装模组则完全不生效 —— 这是这套机制唯一的联机前提。
- 召唤方式照抄原版 `MushroomZombieSpawner.Spawn()`（同样的 `PhotonNetwork.Instantiate(预制体名, 位置, 朝向, 0, null)`），
  所以僵尸的注册（`ZombieManager.RegisterZombie`）、沉睡 / 破土动画、找人、追人、咬人都走原版逻辑，
  连原版「同时最多几只」的上限也照常适用。
- 锁定用的是原版自己的广播 RPC `RPCA_SetCurrentTarget`（`SetCurrentTarget` 只在「目标变了」时才转发，
  而且是 `protected`），各端目标一致，不会出现每台机器各追一个最近的人；强制时长 180 秒。
- 生成点在玩家周围 5 ~ 11 米随机方向，并按预制体上报的「唤醒距离」收窄（保证落在僵尸会醒的范围内），
  再从上方垂直打一条射线贴地 —— 斜坡 / 台阶旁不会把僵尸生在半空或塞进地形。
- 新增配置分组 `防挂机`：`篝火挂机惩罚`（开关，默认开）、`篝火范围（米）`（默认 25）、`触发时长（秒）`（默认 180）。
  回机场（一局结束）时计时表清空，下一局重新数。
- 已知限制：当前这段地图如果**从来没加载过**蘑菇僵尸预制体（没有僵尸生成器、内存里也捞不到），
  这次就召唤不出来，只写一条日志并跳过；一旦在任何一局里见过一次，预制体名字会被记住，
  之后换到没有僵尸的地图也能照常召唤。

### 石化抗性改口径：从「永久免疫」改成「石化值每分钟只涨 1%」

- 这条词条不再让你**完全免疫**石化，改成把石化值的积累压到极慢：**外界**的石化值一点都攒不上来
  （爬石化石壁、撞石化物、护符的 `petrifyPerSecond` 全部无效），但石化值会**自己**以每分钟 1% 的速度慢慢积累。
- 游戏里石化值真正的口径是整数 `CharacterData.petrifyAmount`（0~100，100 = 被石化），所以「每分钟 1%」= 每分钟 +1 点：
  每秒攒 1/60 点、攒够 1 点就通过 `CharacterData.SetPetrify` 加过去（这个入口自带 0~100 夹取与联机同步）。
  于是它从「免死金牌」变成「几乎石化不了、但挂久了还是会被石化」。
- 外界来源仍然是两条路一起拦：`AddStatus(Petrify, …)` 清零、整数镜像 `CharacterAfflictions.AddPetrify` 前缀直接跳过
  （只拦往上加，清空照旧放行）。以前那两处拦下来还要结算代价，现在**只拦不结算**。
- 这条词条的旧代价先后换过两次：最早是「饥饿积累速度 +25%」，随后短暂改成「每挨一次石化就永久掉一截攀爬速度」
  （按开局值线性递减），本版**整条删掉** —— 连同 `ApplyPetrifyWardPenalty`、相关计时器与 toast。积累到多少完全看时间。

### 修复「空灵之体」被其他浮空道具刷掉

- 现象：抽到「空灵之体」后，只要再碰到任何**给浮空的道具 / 效果**，低重力就会在几秒后彻底消失，
  而且这一局再也不会回来。
- 原因：`CharacterAfflictions.AddAffliction` 碰到**同类**状态走的是 `Stack`，而 `Affliction_LowGravity.Stack`
  里有一句 `this.totalTime = other.totalTime;` —— 后来源的时长会**直接顶掉**我们那 999999 秒。
  等那个短时长走完，整条状态被移除（`OnRemoved` → `RecalculateLowGrav`），而空灵之体只在抽到的那一刻挂过一次，
  之后永远不会再挂，于是永久低重力变成「一次性」。
- 修法：空灵之体改成**自我修复**——挂上去之后每秒自查一次（`EnsureLightBody`）：状态还在就再 `AddAffliction`
  一次，走 Stack 把时长顶回永久（`lowGravAmount` 取两者较大值，不会把别人的更强效果改弱）；状态已经不在了就重新挂上。
  回机场清空本局成长照旧会把它摘掉（`ResetForNewRun` 会清空拥有的词条，自查也一并停下），机场里不会带着低重力。

### 拾取去重彻底去掉「按物体身份兜底」+ 吐纳削弱

- 「拾荒者 / 机能零食」的去重键现在只认物品实例 GUID。以前读不到实例数据时会退回网络 ID /
  本机实例 ID，而手↔背包每切一次游戏都会销毁旧物体、重新 instantiate 一个新的，这两个 ID 都会变 ——
  「一直切换物品」于是等于每次切换都重新结算一次（机能零食一直回状态就是这条路径）。
  现在读不到 GUID 就**先不结算**：把这次拾起挂起来，下一帧数据到位了再按 GUID 补结算，最多等 1 秒；
  物品中途离手（丢下 / 收回背包 / 换别的）或到点还没数据就当没捡到。宁可漏给，也不按物体身份误给。
- 「吐纳」削弱：每秒恢复的体力从 `2` 改成 **1%**。体力是 0~1 的小数（`AddStamina` 加完会夹回 1），
  原来那个 `2` 等于每秒把体力条灌满，挂在墙上永远不掉体力；现在每层每秒 1%（最多 2 层），
  词条说明同步改成「每秒自动恢复 1% 体力（每层再叠 1%）」。

### 修复「拾荒者」代币异常（该给的不给、来回切刷币）

- 现象：① 捡起某件物品之后，同类物品再捡就都不给代币了（尤其是捡起来直接塞进背包的那类，怎么捡都不给）；
  ② 在物品栏 / 背包里来回切，代币会一直涨。
- 原因一：物品的「拾起」入口有两个，只有**进手**那条会被结算 —— `Item.PutInBackpackRPC`
  （捡起直接进背包）调的是 `SetState(InBackpack, null)`，`character` 是 `null`，原来的补丁认不出来，
  这条路径从来没发过代币。
- 原因二：去重用的是本机 `GetInstanceID()`，而 Unity 会把已销毁对象的实例 ID **回收给新生成的物品**，
  于是新物品被误判成「已经结算过」（现象 ①）；对象在本地被重建时，同一件物品又被当成新的（现象 ②）。
  更根本的是**物体身份本身就不稳**：手↔背包每切一次游戏都会销毁旧物体、重新 `PhotonNetwork.Instantiate`
  一个（`CharacterItems.EquipSlotRpc` 销毁旧手持物、`BackpackVisuals.RefreshVisuals` 重造背包视觉），
  所以实例 ID 和 PhotonView ID 都会变 —— 真正稳定的只有随槽位数据走的物品实例数据（`Item.data.guid`）。
- 修复：
  - 去重键改成**物品实例 GUID**（`ItemInstanceData.guid`）：切手、切背包、丢掉再捡都是同一个 GUID，
    所以「丢了再捡不重复给」是真的成立；拿不到 GUID 时不结算、也不再退回物体身份（见下一条）；
  - 只有「物品原本不在你背包里」的那一刻才算一次拾取：从背包拿在手上、在背包内挪格子直接跳过；
  - 新增 `Item.PutInBackpackRPC` 结算入口（手满了、物品直接落进背包槽这条路径以前完全不结算）：
    背包视觉体是房主刚生成、实例数据还没下发，所以只从**槽位数据**里读 GUID，读不到就什么都不做
    （宁可不给也不能误给），再判定「放进的是自己背着的背包」；
  - 背包那条路径只做「拾荒者 / 机能零食」两个纯角色侧效果，**不烤**：`FinishCooking` 只有物品控制器
    （房主）调得动，非房主在这里烤等于白调、还会把 GUID 提前占掉，导致物品真正进手时再也烤不上。
  - 「拾荒者 / 机能零食 / 烫手」现在是同一份共用结算（`PickupSettlement`），两条路径都走它，
    去重共用同一个 GUID —— 两条路径之间不会叠加。
  - 该补丁单独成类，即使 `BackpackReference` 结构体参数注入失败也只影响这条路径。

### 修复「背包客」扩容后物品放进去拿不出来

- 现象：滑稽背包因为「背包客」多出两个格子，东西放得进去，但在轮盘里点那两格取不出来。
- 原因：背包里的东西是靠**房主端生成物品模型**时发出的 `PutInBackpackRPC` 登记进 `spawnedVisualItems` 的，
  而取东西正是查这张表（`BackpackWheel.Choose` → `TryGetSpawnedItem`）。生成模型要照外观挂点走，
  挂点只有原版 4 个时房主端会直接跳过第 5、6 格 —— 模型不生成、RPC 不发、表里永远没有那两格。
- 修复：外观挂点跟着数据格数自动补齐，而且是给场上**所有**背包补（房主要替所有玩家生成模型，
  别人的背包挂点不齐一样会卡住）；背上背包的瞬间也会先补一次。

### 选海克斯面板：卡片上移、去掉「[1] 选择」按钮

- 卡片整体上移一点，画面更居中；卡片底边与下方刷新图标之间留出 36px 容错间距，
  悬停时卡片放大上浮也不会误点到刷新。
- 卡片左下角那颗「[1] 选择 / [2] 选择」按钮去掉（整张卡本来就能点，那颗按钮只是重复的视觉引导），
  说明文字顺势往下铺，长词条能多摊两行。键盘 `1` / `2` / `3` / `4` 照旧可用。

### 选海克斯面板刷新按钮改成弯箭头图标并移到卡片下方

- 三选一 / 四选一面板的每张卡片不再把「刷新」文字按钮放在卡片右下角，
  而是改成一张 **↻ 弯箭头图标按钮**，放在**牌面正下方**。
- 按钮在卡片外部，鼠标想点刷新时不会再滑进卡片本身造成误选；
  底部提示文案也同步改成「点卡片下方刷新图标换一个」。
- 图标是自行绘制的 256x256 白色顺时针弯箭头，和主动技能图标同一风格、
  随面板强调色（Accent）着色，已嵌入 dll。

### 削弱主动技能「净化」

- 冷却从 25s 延长到 **60s**。
- 不再顺手把**饥饿**清掉（原先净化等于免费回饱），现在用完后反而额外**消耗 15 点饥饿值**。
- 面板、HUD 里的技能描述同步更新。

### 数值项从滑块改成输入框（输入完自动保存）

滑块拖起来还是不好用 —— 想填 `0.025` 得对着一个像素来回蹭，所以数值项换成**直接输入**：

- 数值行现在是「左边名字 + 右边输入框」：点一下框就能敲数字，
  **回车、点别处、直接关面板都会自动保存**，面板上没有保存按钮。
- 输入框只认数字 / 小数点 / 负号，敲字母没反应；`退格` 删字，`ESC` 是「这次不填了」（配置里的值保持原样）。
- 值和存储都按最多三位小数来（`0.0004` 存成 `0`），免得出现「填进去的和显示出来的对不上」。
- **数值不设上下限**：填多少存多少，不再夹回范围内、也不再弹「只能在 x ~ y 之间」，
  框左边那行范围提示一并去掉（那提示本来就是给夹值用的）。滑块时代的范围计算整套删除。
- 输入框是自己搭的（面板全程手算鼠标位置、没接 EventSystem，UGUI / TMP 的输入框组件在这里跑不起来），
  上一版（`0.3.15`）的滑块与 `Shift` 微调整套代码随之删除。

### `F9` 面板的数值项改成滑块（一排只放一个）

配置面板上的数值项以前是「点一下加一个步长」，想调小只能一路点到上限、等它绕回最小值 —— 实际上等于改不了。

- 数值项（`商店.每分钟代币` / `物价倍率` / `功能溢价`、`行李箱` 三项概率与代币上限、`海克斯.传说词条权重`）
  现在都是**整行的滑块**：一排只放一个，左边是名字、中间是滑轨、右边是当前值；
  **按住左键左右拖，松手那一下才写进配置**（拖动过程只改显示、不落盘 —— 每帧写一次配置等于每帧写文件，会卡）。
- 滑块范围：配置里写了范围的照它来（`功能溢价` 就是 `0 ~ 2`）；没写范围的按默认值量级给一段够用的区间 ——
  整数 `0 ~ 默认值×2`（至少 10）、概率类 `0 ~ 50%`（每格 `0.5%`）、比例类 `0 ~ 100%`（每格 `1%`）、
  倍率 / 速率类 `0 ~ 默认值×4`（每格 `0.05`）。想要更极端的值直接改 `.cfg` 即可。
- 值的显示从两位小数改成三位，`0.025` 不会再被显示成 `0.03`。
- 开关、按键、带可选值的单选不受影响：仍是两列排、点一下切换 / 重绑 / 往后翻一格，右键往前翻。
- 价格行也不受影响：它本来就自带 `−` / `＋` 两颗按钮，方向写在按钮上。
- 拖滑块时**按住 `Shift` = 微调**：步长缩到 1/10（`0.05` → `0.005`），但不小于 `0.001`（数值只显示三位小数）。
  按下 `Shift` 后改成「鼠标横向位移 × 微调步长」累加，鼠标可以拖出滑轨；这次拖动里只要用过 `Shift`，
  后面整段都按位移算，**松开 `Shift` 只是换回粗步长、值不会跳**（否则松手那一下会跳到鼠标位置对应的值上）。
  整数项（`每分钟代币`、`代币上限`）本来就是每格 `1`，不参与微调。
- 面板底部的提示行在微调期间会显示当前步长，停在滑块项上时说明后面也缀了一句用法；
  「按住 Shift 一次 5 枚」的调价行改用同一处 Shift 判定。
- 配置区最后新增一行**「恢复默认」**（右边一颗按钮，和调价段的「重置全部」同一个样式）：
  一键把上面所有数值项调回默认值，拖花了不用一个个拖回去。只动数值项，开关与按键不受影响
  （免得把快捷键一起清掉）；已经等于默认值的项不会重复写盘。

### 修复：机场「禁用海克斯」面板打开时卡顿

面板原来是**在按下按键的那一帧**把 82 行一次性全建出来（每行 1 块底图 + 4 个文字，合计约 490 个对象、
328 个文本）—— 中文文本首帧还要把上千个汉字塞进字体图集，而图集每扩容一次就要重建纹理、让全部文本重算 mesh，
这些活全挤在同一帧，表现就是「打开面板后卡好几秒才缓过来」。

- 改成**在机场里分帧预热**：人一进机场就开始铺这个列表，每帧 4 行，按下按键那一刻列表早就建好了。
- **关键：光把对象建出来是不够的**。TMP 的排版、字形写进图集、网格生成都发生在「渲染前」那一刻，
  而面板的 Canvas 平时是关着的 —— 不主动算一次，这些活还是会被拖到打开那一帧。
  所以每建好一行就立刻 `ForceMeshUpdate()`，整表铺完后再分帧重算一遍
  （预热途中字体图集扩容过的话，TMP 会要求所有相关文本重算 UV / 网格，那一批也在这里做掉）。
- 打开时的兜底只建「可视区 + 4 行缓冲」，其余继续分帧；滚动时按需补齐（每帧最多 12 行）。
  所以「刚进机场马上按开」和「打开后快速滚到底」这两种极端情况也不会退回整表同步构建。
- 玩法没有任何变化，纯粹是打开时的流畅度。

### 修复：游戏更新后模组完全不加载

PEAK 2026-09-07 的更新往 `CharacterAfflictions` 里又塞了一个无参的 `static AddPetrify()`。
「石化抗性」的整数镜像补丁原来只写方法名，Harmony 于是报 `Ambiguous match` 并抛异常 ——
异常抛在 `HextechPlugin.Awake()` 里，后面的 UI 创建全都没执行：游戏里看不到任何模组痕迹，
而 BepInEx 的 `LogOutput.log` 里连错误都没有（只写在 Unity 的 `Player.log`）。
注意这跟 0.3.11 / 0.3.12 的改动无关，纯粹是游戏更新踩到的。

- 「石化抗性」的整数镜像补丁改成用 `TargetMethod()` 精确锁定 `AddPetrify(int)` 签名。
- `Plugin.cs` 不再用 `Harmony.PatchAll()`，改成逐个补丁类打补丁：某一条失败只往 BepInEx 日志写一条
  `[补丁] … 打补丁失败（已跳过，其余补丁照常生效）`，剩下的补丁与 UI 正常起来 ——
  再遇到游戏更新改方法，最坏也只是那一条词条失效，而不是整个模组消失。
- 工具：`_tools/dump-api.ps1` 修掉「某个成员签名读不出来就吞掉该类型剩余成员」的毛病
  （它一度让 `BackpackData.DeserializeValue` 看起来像被删掉了）；
  新增 `_tools/_tmp-harmony-check.ps1`，拿 `_tools/api/members-all.txt` 一次性体检所有补丁目标是否唯一存在。

### 「拾荒者」去掉每局代币上限

- **青铜「拾荒者」**不再封顶（原来每局最多 20 枚）：一件物品一枚，一路捡下去捡多少给多少 ——
  去掉 `DefaultHextechs.ScavengerMaxTokensPerRun` 常量与 `AwardScavengerToken` 里的封顶判断，
  提示也从「本局 x/20 枚」变成「本局 x 枚」。
- 去重规则不变：同一件物品丢掉再捡起来不会重复发（仍和「烫手 / 机能零食」共用 `TryMarkPickupSettled`）。

### 「磁力手」改成「机械手」+「赌徒」调到黄金档

- **「磁力手」整条重做、更名「机械手」**：主动技能不再吸物品，改成**拉长交互距离** ——
  按下后交互距离 **+5 米**（改的是 `Interaction.distance`，拾取 / 开门 / 拉箱子会一起变长），
  **成功交互一次**后立刻复原。冷却 **25 → 60 秒**，而且是**从交互那一刻才开始计时**
  （按下先把手伸着，用掉之后才进冷却；就绪期间再按只给一句提示，不会重复扣能量与石化）。
  代价是按下时立刻 **+5 点石化值**。
- 解锁词条的 id 仍是 `unlock_magnet`：机场禁用面板里存的就是这个键，改了会让老禁用在重启后失效。
- **「赌徒」从传说档调到黄金档**：效果没动（三选一变四选一），稀有度从「权重 1」变成黄金档的 18 ——
  它也是黄金档里唯一一条不挂代价的词条。随之黄金 11 → 12 条、传说 5 → 4 条，
  正面池的分布跟着挪了一点（见 `README.md` 的开箱表）。

### 开箱概率拆成「正面 / 负面」两项 + 黄金档上调

- 行李箱抽奖的 `行李箱.出海克斯概率`（4%）**拆成两个独立判定**：`行李箱.正面海克斯概率`（默认 **2.5%**）
  与 `行李箱.负面海克斯概率`（默认 **1.5%**）—— 正面降下来的那部分全给负面，两边相加仍是原来的 4%。
  抽中哪一半就只在那半边的池子里按品质权重挑，所以两边能分别调。
- 随之**删掉** `行李箱.负面词条权重`（默认 0.4）：负面概率现在由 `行李箱.负面海克斯概率` 直接给，
  那个倍率已经不起作用。老 `.cfg` 里的 `行李箱.出海克斯概率` / `行李箱.负面词条权重` 会在下次启动时被清掉，
  数值不搬（新默认值合计仍是 4%）。想彻底不要负面就把 `行李箱.负面海克斯概率` 填 `0`。
- **黄金档权重 10 → 18**：正面概率砍到 2.5% 之后黄金被摊薄（在「抽到的海克斯」里从 6% 掉到 4%），
  抬到 18 之后回到 **7%**。青铜 / 传说的权重一个没动，多出来的份额是挤掉白银的占比让出来的
  （白银的绝对概率 1.67% → 1.20%）。
- 净效果：负面从「抽到海克斯时 17.5%、每格 0.70%」变成**每格固定 1.5%**（抽到海克斯时 37.5%），
  黄金从每格 0.21% 变成 0.28%。

### 新增：青铜 1 条 + 白银 4 条 + 黄金 2 条 + 传说 3 条 + 负面 1 条

- **青铜「拾荒者」**：每件物品第一次被你拾起时 +1 枚商店代币（**不设上限** —— 本批先写的「每局最多 20 枚」
  随后按玩家要求去掉）。
  去重和「烫手 / 机能零食」共用同一套（`TryMarkPickupSettled`，按物品实例记）——
  同一件东西丢掉再捡起来不会重复给。
- **白银「温室体质」**：夜间寒冷积累 -15%、可叠 3 层。
  原来的青铜「热血体质」和它是同一件事，两条留在池子里只会互相稀释，所以**合并**成这一条；
  id 仍是 `warm_blood`，老存档里抽到过它的人只会发现这条变强了。
- **白银「投掷专家」**：投掷力度 +25%（2 层，与负面「手滑」正对面）。
- **白银「长跑运动员」**：冲刺耐力消耗 -20%（2 层）。和「疾跑者」的省力部分同类，但只省力、省得更狠。
- **白银「不倒翁」**：摔倒 / 被击飞后的硬直时间 -35%（2 层）。
- **黄金「蜘蛛侠」**（原提法的「倒吊人」改名）：挂在边缘 / 绳梯 / 藤蔓上静止不动时不再消耗体力；
  代价：地面移动速度 -12%。
- **黄金「石化抗性」**：外界的石化值一点都攒不上来，但石化值会自己以每分钟 1% 的速度缓慢积累
  （口径改过两次，详见本段顶部那条说明）。
- **黄金「赌徒」**（本批先按传说定档，随后按玩家要求调到黄金）：每次海克斯三选一都变成四选一
  （按要求**不附带任何代价**，是黄金档里唯一一条不挂代价的词条）。
- **传说「不死鸟」**：每局 1 次，摔落伤害不会把你摔到完全昏迷，最多留 1 点伤势。
- **传说「海克斯：机械手」**（原「磁力手」）：解锁主动技能「机械手」
  （45 能量 / 60 秒冷却；按下后交互距离 +5 米，交互一次后复原并开始冷却，代价 +5 点石化值）。
- **负面「倒吊人」**（青铜，只在行李箱抽奖里随到）：抽到立刻清除身上所有负面状态，
  代价是伤势被填到上限的 99% —— 只剩 1% 的余量到倒地线，当场不会昏迷，但一点伤就会倒。

### 这批词条各自挂在哪（实现要点）

- 三选一面板现在能摆 4 张：`HextechPanel` 把整行按 1520 宽等比缩小，
  键位与底部提示（`1 / 2 / 3 / 4`）跟着实际张数动态给。
- 「投掷专家」和「手滑」共用一个补丁：`CharacterItems.DropItemRpc` 前缀里按持有者的词条
  把**蓄力参数**乘一下（两条同时持有就相乘）。
- 「不倒翁」挂 `Character::Fall(float seconds, …)` 前缀，把硬直时长参数缩小 ——
  这个 seconds 会经 `RPCA_Fall` 写进 `CharacterData.fallSeconds`，各端拿到的是同一个缩小后的值。
- 「蜘蛛侠」收在消费端：`Character::UseStamina` 前缀里判断「挂在攀爬物上 + 没有方向输入」→ 这一笔消耗归零，
  后缀再把返回值扶成 `true`（原方法在 `usage == 0` 时返回 `false`，绳梯 / 藤蔓会当成体力见底、把人直接踢下去）。
  （最早盯的 `CharacterData.staticClimbCost` 是错的：那是地形 `ClimbModifierSurface` 的开关，管的是
  「要不要跳过角度 / 地形修正」，挂边缘时游戏根本不碰它 —— 详见本版顶部那条修复。）
- 「石化抗性」两条路都拦：`AddStatus(Petrify, …)` 清零（AddStatus 补丁里单独放行了这一种状态，
  它本来不算「日常负面状态」），以及整数镜像 `CharacterAfflictions.AddPetrify` 前缀直接跳过
  （只拦往上加，清空照旧放行）。两处都**只拦不结算**；词条自己那份「每分钟 1 点」走
  `CharacterData.SetPetrify`，在 `DefaultHextechs.TickPetrifyWard` 里按 `deltaTime` 攒够 1 点再加。
- 「不死鸟」用 `CharacterMovement.CheckFallDamage` 前缀打一个「这次是谁摔了」的标记，
  紧跟着的那次 `AddStatus(Injury, …)` 认领它 —— 不去猜「这一坨伤势是不是摔出来的」。
- 「机械手」改的是 `Interaction.distance` —— 游戏每帧拿它做「一条射线 + 一段球形扫描」找可交互物，
  所以拾取 / 开门 / 拉箱子会一起变长；「这一次是不是真的交互了」判定在 `Interaction.DoInteraction` 上
  （进方法前 `readyToInteract` 为真、出来之后变成假，就是这一帧真的交互了）。
- 传说档不再走品质数值表：`不死鸟 / 机械手` 都是「效果写死、不挂代价」，
  稀有度全靠 `海克斯.传说词条权重`（默认 1）。
- 「倒吊人」是唯一一条**一次性**的负面词条（其余全是乘在基准值上的系数）：
  清负面走游戏自带的 `CharacterAfflictions.ClearAllStatus(false, false)`
  （内部逐个 `SetStatus(…, 0f, …)`，所以诅咒 / 石化 / 蛛网 / 捕蝇草都在内；石化顺带把整数镜像归零），
  紧接着 `SetStatus(Injury, 上限 × 0.99, pushStatus: true)` 填伤势并把清空结果同步给其他客户端。

- **新增 8 条负面（代价）词条**，只会从行李箱抽奖里随到，与原来那 6 条同档（青铜、每层乘在基准值上）：
  - 「僵直」转向速度 -25%（`movementTurnSpeed` / `airMovementTurnSpeed`，2 层）；
  - 「病去如抽丝」中毒 / 孢子 / 尖刺的**消退速度 -25%**（「铁胃 / 硬皮」的反面，2 层）；
  - 「睡不醒」困倦消退速度 -30%（「清醒」的反面，2 层）；
  - 「高烧不退」灼热（晒伤）消退速度 -30%（「防晒霜 / 隔热层」的反面，2 层）；
  - 「手残」拉手距离 -25%（「拉拉手」的反面，2 层）；
  - 「手滑」投掷力度 -30%（2 层，见下）；
  - 「夜盲」每 90 秒短暂失明 4 秒、每多一层间隔减半（2 层，见下）；
  - 「拖后腿」**你 6 米内的队友**体力消耗 +12%（2 层，见下）。
  原先的「晒伤消退变慢」定名为**高烧不退**（描述里也写成「灼热（晒伤）」）。
- 「僵直」与「睡不醒」碰到的新字段（`movementTurnSpeed` / `airMovementTurnSpeed` / `drowsyReductionPerSecond`）
  一并补进 `HextechState` 的基准值，回机场时和原来那几条一起还原，不会残留到下一局。
- 「手滑」挂在 `CharacterItems.DropItemRpc` 的前缀上，**改的是 RPC 收到的「蓄力」参数**：
  这个方法在每台客户端上各自算初速度（`力 = min + (max - min) × 蓄力`），所以只能动参数 ——
  本地偷偷改自己的 `minThrowForce` 会让各端算出来的轨迹不一样，扔出去会来回拉丝。
  蓄力为 0 的普通「放下 / 丢弃」不受影响。
- 「夜盲」直接用游戏自带的失明状态（和黑布条同款），走的是既有的 `AddAffliction` 通道，回机场会被摘掉。
- 「拖后腿」是第一条**效果长在别人身上**的词条：持有者什么都不做，
  判定与加成放在 `Character.AllCharacters` 的距离光环里，由**被拖累的那一方自己的客户端**结算
  （和「领头羊」同一套写法、同一个 `UseStamina` 前缀，两个补丁相乘不冲突）。
- 说明一下开箱概率的变化：负面权重仍是「品质权重 × `行李箱.负面词条权重`（0.4）」，
  所以**负面词条从 6 条变 14 条，开箱吃到负面的机会也就跟着翻了一倍多**。想压回来的话调小
  `行李箱.负面词条权重` 即可（`0` = 抽不到负面）。

- 「围炉谈心」改成**贴着队友**才回体力：触发距离从水平 8 米收紧到**两个身位（3 米）**，
  结算也从「每秒、按人数上档（每名队友 15 点/秒）」改成**每 5 秒回一次、一次回 10% 体力上限**。
  中途有人走远就**中断**：没攒满的那几秒清零，重新挨到一起再从头数 5 秒。
  人数不再分档 —— 身边有几个队友都是这个速率。
- 「机能零食」从「只回伤势与中毒」改成**每种负面状态各减少各自当前值的 5%**（每层再叠一份）：
  拾起一件物品时，伤势 / 中毒 / 孢子 / 寒冷 / 灼热 / 困倦 / 饥饿 / 诅咒 / 荆棘 / 蟹钳
  逐项按自己的条削掉 5%（相对削减：条越长扣掉的绝对值越多，也不会被一次扣空）。
- 「我还有光」改成**石化值过半才开始转化**：石化值不到石化上限的一半时原样留着（该被石化的压力还在），
  超出一半的那部分才按原来的速率（每层每秒 4% 石化上限）慢慢转成体力，先补「额外体力」、满了再转体力。
- 开箱抽出**负面（代价）词条的概率略微提高**：`行李箱.负面词条权重` 默认值 **0.35 → 0.4**。
  玩家自己改过这一项的以自己 `.cfg` 里的值为准。
- **配置改成「以新版本为主」**：装上更新之后第一次启动游戏，模组会把 `.cfg` 里的数值项
  （代币、物价、开箱概率、抽取权重）**刷回本版的默认值**，按键 / 开关 / 更新源保持玩家自己的设置。
  以前 BepInEx 只在「键不存在」时才写默认值，改默认值对老玩家等于没改 —— 上面那条 `0.4` 就撞上了这件事。
  做法是新增一条内部配置 `内部.配置版本` 记账：和当前版本对不上（首次安装 / 更新 / 降级）才覆盖一次，
  同一个版本内玩家在面板里调过的值照旧保留；覆盖结果会写进日志（`BepInEx/LogOutput.log`）。

- **四条状态词条调数值**（「1 点」现在统一按**满条的 1% = 内部 `0.01`** 算，层数直接乘上去）：
  - 「饱腹」升到**白银**档，改成**每 60 秒缓解 1 点饥饿**（原青铜、每 2 秒 `0.015`）—— 1/2/3 层是每分钟 1/2/3 点；
    原来的写法每 2 秒就动一次条子、量级也偏重，现在整分钟结算一次。
  - 「自我修复」改成**每 10 秒恢复 1 点伤势**（原每 2 秒 `0.012`）、「温暖之躯」改成**每 5 秒驱散 1 点寒冷**（原每 2 秒 `0.02`）。
    换算成速率：自我修复每秒 `0.001`（原 `0.006`）、温暖之躯每秒 `0.002`（原 `0.01`）—— 都比原来慢得多，慢得不合适就调这两个秒数。
  - 「大胃王」从饥饿积累 **+50% 降到 +25%**。
  - 「冷血体质」从夜间寒冷 **+60% 降到 +20%**，但补上半个好处：**受到的灼热（晒伤）积累 -50%**
    （每层再乘 `0.5`，2 层 -75%），走的是四条专项状态抗性那套 `AddStatus` 前缀削减。

- 发布脚本（`_tools/publish.ps1`）**不再每次发版都动安装器的版本号**，也不再提示「安装器 exe 比模组旧」：
  安装器已经改成云更（联网从更新服务器拉最新版模组），不必跟着模组一起发版 —— 之前那条提示会
  让发布后的必然动作变成「关掉正开着的安装器再 build 一次」，而它往往正开着、直接把构建卡死。
  安装器版本号退回 `1.1.4`（本次发版涨上去的 `1.1.5` 并没有对应的 exe 产物）；今后只有安装器
  自身的代码有改动时才手动抬版本、单独 build 一次 exe 发给玩家。

- **开箱不再逐件播抽奖动画**：行李箱抽到的**物资与代币**改成当场结算 —— 东西直接掉在箱子原位、
  代币直接进账，只有抽到**海克斯**的那几次才播那段横向滚动动画。一箱动不动抽十来件，
  件件都要等滚动条走完太拖时间（按玩家反馈改的）。
- **海克斯抽奖动画提速**：一次从约 3.6 秒压到 2.6 秒（滚动 `2.2 → 1.5` 秒、落定 `0.35 → 0.28`、
  停留 `1.05 → 0.85`），商店的三连礼包连着播 3 遍时体感差别最明显。

- **`F9` 设置面板改成两列**：配置项与商店调价行都按两列排下去（面板内容宽 1200，一行只放一件
  等于白空一半位置，列表还被拉得老长），分组标题、调价表头、「重置全部」仍然占满一整行。
- **`F9` 面板里的话砍掉一半**：每一行只写配置项的名字，整句说明挪到面板底部的说明行 ——
  鼠标停在哪一行就显示哪一行的说明；商品调价行不再重复「某某物资 · 」这种类别前缀
  （上面的小标题已经写着），副标题和「重置全部」那行的说明也一并压缩。

- **商店物资价格重做**：改成「稀有度基准价 + 功能底线价」，只抬「作用很大、价格却低得离谱」的那批，
  不再按稀有度一条直线走：
  - **功能底线价**：绳子 / 枪绳 / 藤枪 / 气球 / 魔豆 / **踏板菇** / 滑翔翼 / 喷气・火箭背包 / 降落伞 /
    攀爬钉 / 可放置类补到 **30 枚**，护符（大治疗 / 无限体力 / 二段跳 / 复制）与救援钩 **30 枚**，
    背包・号角・护盾玩偶 **25 枚**，提灯 / 信号弹 / 蜡烛 / 火把 / 炸药 / 蜂巢 / 雪球 / 曼德拉草 **20 枚**。
    底线是**补差价**：按稀有度算下来比它便宜的才被补到线下，本来就比底线贵的（比如荒诞稀有度的提灯）
    一分不涨 —— 涨价只落在「很有用却便宜得离谱」的那批身上，整家店不跟着变贵。
    判定方式是看道具身上挂了哪些游戏组件，所以不依赖物品名、也不依赖语言。
  - **功能顶价**（反方向的一条）：滑翔翼 **22 枚**、一束气球 **15 枚** —— 稀有度把它们顶到 30 往上，
    但作用配不上那个价签，按稀有度算得更贵的一律压下来；本来就便宜的原样不动。
    价格表现在是**从上往下第一条命中的生效**（不再取最高），所以点名的那几件排在所属大类前面。
  - **纯补给封顶**：蘑菇 / 浆果 / 袋装食品（没有别的功能时）在基准价 `×0.8` 之外再按住 **12 枚** ——
    传说档的一颗果子按稀有度算下来比一捆绳子还贵，这类虚高一律压掉。
  - **稀有度基准微调**：普通 / 精良 / 稀有 / 史诗 / 传奇 / 神话 / 荒诞
    从 `4 / 7 / 12 / 18 / 28 / 45 / 70` 改成 `5 / 8 / 12 / 16 / 20 / 24 / 30` ——
    底下两档各抬 1 枚（以前 4 枚就能买一件普通物资，比一次抽奖还便宜），稀有及以上整体下调
    （一局几十枚代币，70 枚一件等于买不到）。除功能性道具外，整店只降不涨。
  - 食物（蘑菇 / 浆果 / 袋装食品，且没有别的功能）按基准价的 `0.8` 卖，并且不超过 12 枚。
  - **同档不再一口价**：每件物资再按 prefab 名的哈希算一个 `0.88 ~ 1.24` 的稳定浮动，
    所以同一档里每件东西的价签都不一样；用名字哈希而不是随机数，是为了让房主和客户端算出同一个价、
    且重启游戏后不变。
  - 新配置 `商店.功能溢价`（默认 `1.0`）：`0` = 底线与顶价都不管、只按稀有度定价，`1` = 补到 / 压到上面的价，
    `2` = 把这份差距再翻一倍。
  - 建完物资表后往日志里打一句总结（认出的分类数量 + 最贵的前 10 件），方便回头核对归类对不对。
  - 逐件调价表、`商店.物价倍率`、联机时「价格由房主统一」这些规则都没变。

- 修复**在 `F9` 设置面板里关掉「启用模组」之后**的两件事：
  - 屏幕上已经开着的界面现在会跟着一起收掉 —— 右侧 HUD、技能条、商店、三选一、禁用面板、更新提示。
    以前只有模组逻辑停下、HUD 还留在屏幕上，看起来就像开关没生效。三选一按「玩家自己先收起来」处理，
    机会留在账上，重新打开模组后按「重新打开三选一」的键还能接着选。
  - `F9` 现在始终能打开设置面板 —— 以前关掉模组后主循环直接 `return`，`F9` 再也按不出面板，
    只能重启游戏才能改回来，等于把自己锁在外面。设置面板是关掉模组之后唯一还能用的入口，
    所以关掉的那一刻故意不把它一起收掉（不然玩家刚点的那个开关会连面板一起消失，连在哪改回来都看不到）。

- 行李箱开出海克斯的概率**再砍掉六成**：`行李箱.出海克斯概率` 默认值 `0.1` → `0.04`（10% → 4%）。
  开箱主要还是拿物资，强化不再那么容易靠开箱子堆出来。老配置里存着旧默认值 `0.1` 的会自动迁到 `0.04`，
  自己调过这个值的不动（出代币概率没变，仍是 10%）。

- 新增**屏幕正下方的技能 HUD**：已解锁的主动技能排成一排，一格一个技能，显示技能图标与这一格的
  能量消耗；冷却中会把图标压暗、盖一层从上往下退去的遮罩，并在中间显示剩余秒数（不足 1 秒带一位小数），
  当前按 `H` 停在哪一个就用金色描边标出，最上面一行写着当前技能名与按键提示。
  能量不够时消耗数字变红、图标压暗；长按 `K` 把整个 mod UI 藏起来时它跟着一起消失。
  图标取自开源图标库 **game-icons.net**（CC BY 3.0，作者 Delapouite / Lorc 等），
  构建时嵌入 dll，所以模组仍是单文件分发。

- 配置面板的快捷键默认值从 `F1` 改成 **`F9`**（`F1` 容易被别的程序先吃掉，按了没反应）。
  老配置里存着旧默认值 `F1` 的会自动迁到 `F9`，自己改过键的不动。面板内容照旧：
  里面能直接改这份配置里的每一项，只是打开方式换了个键。
- **删掉调试功能里的「强制三选一」**：它能无视篝火凭空弹一次三选一面板，属于能绕过正常流程的口子，
  正式版本不该留着。对应的 `调试.强制三选一按键` 配置项一并移除（老 `.cfg` 里那两行会被清掉），
  三选一现在只会由游戏自己的流程（篝火 / 词条触发）弹出，且只弹给本机玩家。
- 商店支持**单品调价**，入口在 **`F9` 配置面板最底下的「商店调价」段**：每件商品一行，
  左边名字 + 默认价，右边 `− 现价 ＋`。点一下改 1 枚（按住 `Shift` 一次 5 枚，范围 1 ~ 999），
  改的是这件商品自己的基础价，和配置里的全局「物价倍率」叠加；点中间那串数字单独恢复这一件，
  表头下面那行右边的「重置全部」一键全恢复。商店里按 `T` 的快捷调价模式还在（同一张表，
  卡片上直接多出 `−` / `＋`），两边改的都是 `BepInEx/config/HextechMod.prices.txt`
  （单独一个文件，不塞进 `.cfg` 里）。物资那几百行要等物品表被游戏扫出来（进游戏开过一次商店）
  才会列出来 —— 面板不去替商店决定「什么时候扫」，扫描失败会被商店记成「这局不卖物资」。
  **联机时只有房主能调价**：客户端的面板标题写着「联机时只有房主能改」、按钮点了只弹提示，
  房主改完会把整张表广播给全房间，中途进房的人主动向房主补齐一份，房间里的价格全场统一。
- 安装器**不再内置 mod 本体**：从这一版开始，安装 / 更新一律联网从更新服务器拉最新版
  （`version.json` → dll → SHA256 校验）。好处是安装器不用跟着模组一起发新版，
  代价是没网就装不了 —— 失败时会直接说明是网络问题，而不是拿一份旧 dll 糊过去。
  对应的发布脚本（`_tools/publish.ps1`）也带上 `-p:DeployModFiles=false`：
  发布不再顺手改本地游戏目录，装 / 更 mod 只走安装器。

- 「背包客」的扩容改回**只有滑稽背包算数**：普通背包原版就 4 格（扩了也看不出多），
  喷气背包 / 火箭背包本身没有物品格，火箭背包背上之后按住 E 是点火，动它的格子会出怪状态。
  现在扩容（数据格、轮盘切片、模型挂点、序列化）只对「本地玩家背着的滑稽背包」生效，
  别人的背包、地上的背包、别的背包类型一个补丁都不碰，完全走原版。
  老版本补丁给普通背包 / 火箭背包扩过容的，会在穿戴、开轮盘、同步时缩回原版 4 格。
- 修掉「火箭背包脱不下来，按 4 会凭空掉下来一个、自己身上的还在」：根因是
  `BackpackVisuals.RefreshVisuals` 的循环上界被补丁换成了「数据格数」，而循环体写的是
  `backpackSlots[i]` —— 数据被扩到 6 格、挂点还是原版 4 个时直接越界，异常抛在
  「背上 / 卸下背包」的协程里，收尾就断了。现在上界取「数据格数」和「挂点数」里小的那个，
  不可能越界；背包背上时也会顺手把挂点补齐。

- 配置面板的入口从 **ESC 主菜单** 挪进 **ESC → 设置**：游戏原生设置页（主菜单 → 设置 也是同一个
  页面）最上面多了一行「海克斯模组设置」，点它打开配置面板；快捷键见下面那条（现在是 `F9`）。
  做法是照着一行原生设置格克隆一份、换成我们的标题和「点击打开」按钮 —— 字号、行高、配色、
  缩进跟旁边那些原生设置项完全一致，挂在 `SharedSettingsMenu.ShowSettings` 之后重插
  （它每次切标签页都会重建格子列表）。每插一行都置顶，免得一屏放不下时被排到看不见的地方。
  原来 ESC 主菜单里那个按钮去掉了（不需要了）；旧的 `界面.暂停菜单里加入口` 配置会自动接到
  新的 `界面.设置里加入口` 上，老配置不会丢。

- 「疾风步」加了代价、冷却也拉长了：使用后立刻涨一截饥饿（`0.1` 内部值，大约是饥饿条的一成，
  和游戏自带测试指令 `AddHunger()` 的 `+0.2` 同一个量级），冷却从 `15s` 改成 `30s`。
  以前这技能是「5 秒无限耐力」的纯收益，现在冲刺不耗体力但会饿、也放不了那么勤。
  饥饿走游戏自己的 `CharacterAfflictions.AddStatus`（参数照抄它积累饥饿时的那次调用），
  所以状态同步、音效、「无敌」期间被拦下、「不饿」buff 让它失效这些原版规则都还在。

- 修复「背包客」扩容出来的格子仍然叠在一起：上一版那段「把整圈格子重新均分」的重排
  **其实是空转** —— 它读 `slices[i].transform.localPosition` 当基准，而这些切片在轮盘打开的那一刻
  读出来全都在同一个位置（日志里就是那句「背包轮盘的格子没有围成圆」），于是直接 return，
  克隆出来的第 5、6 格还留在第 4 格的位置上，正好压着。
  现在改成：**只把扩容格（`slices[5..]`）摆到轮盘正上方一排**，原版那 4 格、捡背包那一片、
  喷气背包的油量表一概不碰；位置按世界坐标算（父级挂在谁下面、转了多少度都不影响），
  格子间距取切片自己的矩形尺寸，所以不会互相重叠。
  位置改用 `anchoredPosition` 写（直接写 `localPosition` 会被锚点算回去），
  并且**每帧（`Update` + `LateUpdate`）都摆一遍** —— 轮盘打开的过程中格子位置还会被游戏自己改写，
  只摆一次就会被盖回去。另外顺手把背包外观挂点也改成按世界坐标量，
  量不出原挂点圈时用兜底间距，不会再把多出来的物品模型塞到圈心。

- 「围炉谈心」改成**和队友抱团**回体力：不再要求站在篝火旁 —— 只要附近（水平 8 米内，
  不算高度差，上下两层贴着站也算）还有活着的队友，就每秒回复一次体力，并且**按人数上一档**：
  1 名队友 15 点/秒、2 名 30 点/秒，以此类推。倒地的队友不算数（守着尸体不回体力）。
  原来「叠 2 层、每层每秒 2 点」的层数机制一并去掉，阶梯只按人数走。

- 修复「烫手」仍会烤非食物、并且把已经烤过的东西再烤一遍。现在是两道闸：
  ① **只认食物** —— `itemTags` 里带 `PackagedFood` / `Berry` / `Mushroom` 才算，
  火把、木头这类带 `ItemCooking` 的非食物一律不碰（有的还有 `wreckWhenCooked`，烤一下直接坏掉）；
  ② **每件只烤一次** —— 出生就是熟的（`preCooked`）或已经被烹饪过的（`timesCookedLocal > 0`）
  不再动手。以前只拦「烤到 `COOKING_MAX`」，所以半熟的东西会被反复烤到糊 / 炸掉。
  描述也同步改成「每当第一次拾起食物时，立刻把它烤熟」。

- 修复「海克斯设置点不开」：入口是从 ESC 暂停菜单里点的，点下去的那一刻游戏正处在暂停 / 取消状态，
  而我们的宿主窗口之前声明了 `closeOnPause` / `closeOnUICancel` —— 窗口刚压进
  `MenuWindow.AllActiveWindows` 的下一帧就被这两条规则直接关掉（`Player.log` 里能直接看到
  `opening window` 紧跟着 `HextechWindow closing.` 反复出现），玩家看到的就是「点了没反应」。
  现在这两条改成 `false`，并且入口会先把游戏原生的窗口（暂停菜单本身）关掉再打开面板；
  按 ESC 收起各界面的工作由面板自己负责（每个面板的 `Update` 都自己认 ESC，全收起后由每帧兜底的 `Sync` 关窗口）。

- 禁用海克斯的快捷键从 `B` 改成 `M`（配置项 `界面.禁用海克斯`）：`B` 和游戏里一些原版交互太容易撞。
  老配置里存着旧默认值 `B` 的会自动迁到 `M`（只迁正好等于旧默认值的，自己特意设过的不动）。

- 新增「本局还没有禁用海克斯」的顶部提醒：只要人在机场、而且这一票还没投，
  屏幕上方就会挂着一条提示（`本局还没有禁用海克斯 · 按 M 打开禁用面板`），禁用完或离开机场自动消失。
  禁用是每人一票、本局全房间生效的，而这件事只有回机场才能做，之前完全靠玩家自己记得。

- 「禁用海克斯」面板改成**一排一个词条**：左边品质徽标、然后是名字，后面跟该词条的效果说明，
  最右边是「我禁的 / 队友禁的」状态。原来三列平铺，名字和效果都读不全。

- 商店买到的物资不再摆在地上：房主实例化之后直接走原版 `Item.RequestPickup`
  （内部就是 `Player.AddItem` + 同步全房间 + 通知买家 + 销毁地上那个房间物件），直接进买家的物品栏，
  和玩家自己弯腰捡起来的结果完全一样。物品栏满了才会留在原地（这时也给了明确日志，能自己捡）。
  之前掉在地上会滚、会被队友顺手捡走、会滚进地形，玩家经常以为「买了没给」。

- 修复「火箭背包背上后脱不下来」：`BackpackWheel.InitWheel` 的物品格循环上界是
  `itemSlots.Length`，循环体两个分支都无条件写 `slices[i + 1]`，所以只要「背包格数 > 轮盘切片数 - 1」
  就必然越界；越界异常被 Finalizer 吃掉、轮盘被关掉，之后每次开都抛 —— 而**捡 / 卸背包就在轮盘第 0 片**，
  于是表现成「背包脱不下来」（轮盘里还能看到背包里的东西）。现在两头都堵上：
  扩容前先确认轮盘切片补得齐（补不齐就只扩到轮盘放得下的格数，绝不扩数据），
  并且给 `InitWheel` 加了一个 transpiler 把循环上界夹成 `min(格数, 切片数 - 1)` ——
  少显示几格只是难看，打不开是玩不了。

- 修复「在机场按 `B` 禁不了词条」：机场判断原来是用「没有 `MapHandler` 就当在机场」反推的，
  但机场场景里本来就带着 `Map` 那套物体，`MapHandler` 也存在，于是同一个键在机场反而被拦下来，
  只会弹一句「只能回到机场再禁用词条」—— 怎么按都开不出面板（面板自己 `Update` 里也会立刻 `Hide`）。
  现在统一改成比对当前场景名（和游戏自己的 `GameUtils` 一个口径）：
  禁用面板在机场可开、一离开机场自动收起；「禁用只能在机场」这句判断不再误伤。
  顺带：没有本地角色时（刚进机场、角色还没生成）按 `B` / `F1` 也不再毫无反应了。

- 修复合着机场开局时看不到「机场里可以按 `B` 禁用本局不想看到的词条」这句提示：
  BepInEx 是在第一个场景加载完之后才把插件装起来的，直接启动游戏落在机场的话，
  `sceneLoaded` 那一枪早就放过了，清局逻辑（连带这句提示）永远不触发。现在启动时会补一次。

- 修复代币在机场也会累积的问题：判定同样是那个 `MapHandler` 反推，一并换成场景名判断。
  机场（主菜单 / 大厅）不再发代币，局内计时也同步对齐。

- 修复商店「物资」分类在机场买了个空的问题：物资是 Photon 的「房间物件」，
  在机场买下会掉在机场那套世界坐标上，上岛之后不会被清掉但位置和岛屿地形完全对不上，
  玩家根本捡不到 —— 等于白花代币。现在机场里这类商品直接置灰，点击会说明「上岛之后再买」，
  海克斯抽奖那一类不受影响（它发的是词条，不是地上的东西）。

- 商店掉落链路补齐了与原版 `Spawner.InitializePhysics` 一致的同步（`ForceSyncForFrames` + `SetKinematicRPC`），
  并把原来「实例化失败就静默 `continue`」改成明确的日志：Photon 默认预制体池是按
  `0_Items/<名字>` 去 `Resources` 里找的，找不到只会 `LogError` 后返回 null ——
  以前玩家只会看到「已购买」而地上什么都没有，无从排查，现在日志里能直接看到是哪个名字没找到。

- 修复「烫手」：现在只对食物（可烹饪物品）生效，且每件物品只结算一次 ——
  以前把物品从背包里拿出来也会触发（`Item.SetState` 对「取出」同样触发），拿进拿出几次就把东西烤到糊、烤到有毒。
  现在按物品实例去重，塞背包、拿出来、丢地上再捡回来都只算一次。

- 修复「机能零食」：每件物品只生效一次，丢地上再捡也不会重复获得收益；恢复量减小，
  并且不再清除全部负面状态 —— 以前挂的是游戏里「治疗全部状态」的 `Affliction_HealAll`，
  等于捡一次东西就把饥饿、寒冷一起清空；现在只减「伤势」与「中毒」两项。

- 长按 `K`（配置项 `界面.收起展开面板`）可以把整个海克斯 UI 藏起来，连收起后那颗角落小提示
  和顶部提示条一起不画，屏幕上一点 mod 的痕迹都没有；再长按一次原样恢复到藏起来之前的状态
  （之前是收起的就还是收起）。短按仍然是展开 / 收起右侧面板。

- 新增「禁用海克斯」面板（机场按 `B`，配置项 `界面.禁用海克斯`）：列出全部词条，
  每位玩家可以禁掉一个，被任何人禁掉的词条本局全房间都不会再刷出来（三选一、商店、行李箱都算）；
  点自己已禁的那张可以取消，点别的卡片则改投。一局结束回到机场自动全部解除，下一局重新选。
  名单走 `PunRPC` 全房间同步，中途进房间的人会向房主问一次、自动补齐当前名单。

- ESC 暂停菜单的原生「设置」按钮下面新增一个「海克斯设置」入口，点开即可在游戏内调整模组的所有配置
  （开关点一下切换、数值点左右加减、按键点一下再按新键），改完立刻生效并存盘。
  入口是克隆原生按钮做的，样式与原生一致；万一和游戏版本合不来，可用配置项 `界面.暂停菜单里加入口` 关掉。
  另外保留 `F1`（配置项 `界面.打开海克斯设置`）作为备用入口。

- 配置页整页中文化：mod 管理器（BepInEx ConfigurationManager 这类前置）的配置页显示的是
  「分组 → 条目 → 当前值」，描述要悬停才看得到，而原来的分组和条目都是英文
  （`General.Enabled`、`Skill.SkillKey` …），所以玩家打开就是一页英文。
  现在分组改成 `常规` / `技能` / `界面` / `商店` / `行李箱` / `海克斯` / `更新` / `调试`，
  条目名一并中文化。老配置会在插件启动时自动搬到新键上（读完旧键的值就把它从 .cfg 里删掉），
  按键绑定、更新源、已忽略版本这些不会被重置回默认值。

- 右侧 HUD 可以用 `K` 展开 / 收起了（配置项 `界面.收起展开面板`）。收起后整块词条面板藏起来，
  只在右边留一颗小提示，内容为 `K 打开菜单`、`L 打开商店` 与当前代币数 —— 不会再挡住右边的画面。
  面板展开时标题栏右侧常驻一句 `按 K 收起面板`（按键改了跟着变），收起 / 展开各弹一条 toast，
  收起状态在整局内一直保持。

- 新增图形化一键安装器（`src/HextechMod.Installer`，产物 `HextechModInstaller.exe`）：
  模组本体内嵌在 exe 里，单个文件即可分发。能顺着 Steam 的库列表自动找到 PEAK 安装目录，
  缺少 BepInEx 时自动从 Thunderstore 下载安装，一键安装、一键卸载。
  界面配色与排版复用模组在游戏内的面板（`UiFactory`），目标框架 .NET Framework 4.8，
  Win10 1903+ / Win11 免装运行时。
- 修复安装器把「没开游戏」误判成「PEAK 正在运行」的问题：原来只比对进程名，
  机器上只要有任何一个叫 `PEAK.exe` 的进程就算数 —— 装在另一个盘的另一份游戏、
  别的同名程序、或者最典型的「上次退出时卡在后台没走干净的残留进程」，
  都会让人明明没开游戏却被拦下来。现在会核对进程的可执行文件路径是否真落在游戏目录里；
  碰到只有残留进程的情况也不再直接卡死，而是问一句要不要结束它，一键清掉继续装。
  顺带把游戏确实在跑时的提示写清楚了（窗口关了但进程还在，该去哪里结束）。
- 安装器窗口一开就联网问一次服务器上的最新版本，把版本号和更新公告打在日志区，
  「HextechMod」那一行同时标出「未安装 · 将安装在线版 vX」/「已安装 · vY · 可更新到 vX」。
  问不到只记一行日志，不影响离线安装。

- 修复安装器对「以前用雷霆商店 / r2modman 装过 mod 的玩家」装完没效果的问题：
  那类玩家的游戏目录里只剩 `winhttp.dll` + `doorstop_config.ini`，而配置里写的是相对路径
  `BepInEx\core\BepInEx.Preloader.dll`，框架本体其实在 Mod Manager 的 profile 目录下 ——
  只有从 Mod Manager 启动时才由它把路径指过去。安装器原来把 profile 里那份也算作「框架已安装」，
  于是跳过下载、把 mod 也装进 profile；玩家从 Steam 启动时 doorstop 照相对路径什么都找不到，
  整套框架等于没加载，看起来就是「装了个寂寞」。现在只认「从 Steam 启动真能加载到」的那一份
  （在游戏目录里，或由 doorstop 明确指到），profile 里那份会触发把完整框架补装到游戏目录；
  mod 同时往 profile 同步一份，所以从 Steam 和从 Mod Manager 启动都能用。
  安装器界面也会提前把这种情况和修法写清楚，`HextechMod` 那一行同样会标出
  「只在雷霆商店 profile 里 · 从 Steam 启动不生效」。卸载时两边的副本都会清掉，
  而框架装在游戏目录之外时不会越界删除。

- 修复三选一面板按 `ESC` 会**永久吞掉这次强化**的问题：原来管理器在弹出面板时就把这次从待选队列里
  出队了，而关掉面板又不回调任何人 —— 于是按一下 ESC，那次 3 选 1 就凭空消失了，玩家少一次强化。
  现在面板区分「选过了」（`Hide`）和「没选就关掉」（`Dismiss`），只有真的选中才出队。
- 新增按键 `Y`（配置项 `界面.重新打开三选一`）把还没选的面板随时叫回来 ——
  面板弹出来时可能正被怪追着打，玩家可以按 `ESC` 先收起来、安全了再选。
  玩家自己收起来的面板不会过一会儿自己再弹（否则按 ESC 等于没按）。
- 跳过的三选一会**叠加**：没选就一直欠着，下一次点燃篝火时连同新的一次一起弹出来
  （跳过 2 次就攒成 3 次连选，以此类推），标题里会写明总共欠了几次；
  连着弹时两次面板之间隔 1.2 秒，让人看清刚拿到的是什么。回到机场开下一局才清空。

- 版本检查时机提前到**刚打开游戏、还在主菜单（还没选在线 / 单人）**的时候，
  而不是进了对局才查：那会儿玩家还没开始玩，弹提示不打断操作，也不会跟登岛三选一抢屏幕。
  等主菜单场景出现后再缓 2 秒才弹；万一场景名对不上，25 秒兜底照查，不会把提示卡没。
- 修复更新提示框**一闪而过、根本点不到**的问题：弹窗 `Push` 开窗口之后，
  `HextechWindowHost` 每帧的兜底 `Sync` 会因为「没看到任何界面开着」立刻把窗口关掉，
  弹窗下一帧自查时发现窗口没了就自动收起。现在兜底判据把更新弹窗也算作「界面开着」。
- 更新提示里「已忽略」的 toast 和服务器公告里引用的还是旧英文键名 `IgnoredUpdateVersion`，
  玩家照着去配置页根本找不到，现在都改成实际的中文键名 `更新.已忽略版本`。
- 服务器地址不再硬编码进源码：模组和安装器的更新源改由本地 `Config.Build.user.props`
  的 `HextechUpdateFeedUrl` 提供（该文件已被 `.gitignore` 忽略），构建时以程序集元数据注入。
  源码、模板、文档、日志和游戏内提示里都不会再出现真实地址；没配的时候两端都跳过版本检查。
  顺带把 `更新.更新源地址` 的默认值改成空 —— 免得 BepInEx 把内置地址抄进玩家的 `.cfg`。
- 游戏内的更新提示**不再带「立即更新」按钮**，改成写清楚去哪更新：弹窗只留「忽略此版本 / 稍后再说」，
  正文下方常驻一行「更新方法：退出游戏，运行安装器 `HextechModInstaller.exe`，选中同一个游戏目录后点一键安装」。
  原因是进程内根本换不掉自己：插件 dll 正被游戏加载着（文件锁死），原来那套只能「先把新版下到
  `HextechMod.update\`、再起 bat 等 `PEAK.exe` 退出后覆盖」，游戏装在 `Program Files` 下要有管理员权限
  才写得动、静默替换游戏目录里的 dll 容易被杀软拦、替换失败还会留下半新半旧的 dll；安装器干同一件事
  是在游戏关着的时候跑的，稳得多。随之下载、SHA256 校验与退出后替换的脚本代码一并删除。

- 修复「背包客」扩容后背包轮盘格子重叠的问题：原来只重排物品格 `slices[1..]`，
  第 0 片留在原地，物品格把整圈占满之后就被压在相邻格上。现在把**全部切片（含第 0 片）**
  一起在同一个圆上均分，半径按「相邻弦长不小于原版」放大，所以扩到 6 格也不会重叠。
  另外重排时只算 `InitWheel` 真正会用到的那几片，预制体里多余的富余切片不再把可见格子挤窄。
- 抽奖动画改成**排队播放**：动画还没播完就再买一张券、或者再开一个行李箱，
  新抽奖会接到队尾等当前这次演完再播，而不是像原来那样被直接丢掉。
  多次播放的收尾回调（结算播报、商店刷新）会串起来一起执行。
- 右侧 HUD 再往上挪 30 像素（`PanelY` -206 → -176），仍然没顶到右上角那排原生 UI。
- 右侧 HUD 的词条列表**分页轮播**：一页最多 7 条，超过就自动翻页（**每 10 秒**换一页，循环），
  标题栏同时显示 `海克斯 HEX · 1/2` 这样的页码。行高按「一页 7 条」算，
  所以分页时面板高度不再忽大忽小；新抽到的词条仍从第一页开始显示。
- 白银词条的抽取权重 **30 → 22**（青铜 60 / 黄金 10 / 传说 1 不变），白银不再那么满街都是。
- 抽奖动画总时长从约 5.4 秒压到约 3.6 秒（滚动 3.4→2.2、停格 0.5→0.35、结算停留 1.5→1.05），
  三连礼包那种连续播 3 遍的场合不再熬人。
- 商店的海克斯抽奖券整体**涨价 1.5 倍**（青铜 12→18、白银 30→45、黄金 60→90），
  「强化礼包 · 三连」涨价 10 枚（45→55）；并且这四件**整局每件限购 1 次**，
  买过之后卡片置灰、提示「本局限购，已买过」，回机场重开一局才恢复。
  一局最多从商店拿到 6 项强化（3 张券 + 三连礼包），想再要多只能靠点燃篝火和开行李箱。
- 新增白银词条「皮糙肉厚」（受到的伤势积累 -30%）、「抗毒体质」（中毒 -30%），
  青铜词条「隔热层」（灼热 -20%）、「破咒者」（诅咒 -20%）：
  和「坚韧之躯」共用同一套 `AddStatus` 前缀，但只削一种状态，所以每层比通用的 25% 重一档，不走品质表。
- 新增黄金词条「百毒不侵」：孢子积累封顶在僵尸化阈值的九成，并按掉 `willZombify` 的取值，
  代价是地面移动速度 -10%。
- 新增青铜词条「围炉谈心」：站在点着的篝火旁时每秒回复 2 点体力（可叠 2 层）。
- 新增白银词条「收藏家」：商店代币的积累速度 +50%（可叠 2 层，两层约等于商店全场半价）。
- 新增白银词条「资本家」：每层每分钟额外获得 0.5 枚商店代币（可叠 2 层）。
  这是绝对收入、不随配置里「每分钟发几枚」缩放，所以和「收藏家」一样不走品质表。
- 新增白银词条「抬轿人」：扛着倒地的队友时移动速度 +15%（可叠 2 层）。原版没有「扛人减速系数」
  这种能直接改的字段，所以等价地做成「扛人时加速」。
- 新增白银词条「送葬人」：附近 10 米内有队友倒地时，回复 30 点体力并获得 6 秒加速。
- 改名：「海克斯：跳跃」/ 技能「海克斯跳跃」→「海克斯：漂浮」/「海克斯漂浮」（效果是朝视线弹射 + 1.2 秒低重力，跟跳跃无关）；
  全量核对了一遍词条名与效果，其余没有发现对不上的。
- 新增品质档「传说」：一次加成 25%，抽取权重 1（可配 `海克斯.传说词条权重`，0 = 抽不到），徽章 `※`、紫色；
  商店的三档抽奖券与三连礼包都封顶在黄金，所以传说只能靠三选一 / 开行李箱自然抽到。
- 「骨质疏松」从白银升到**传说**，并加上「本局全队最多 2 人拿到」的限制：
  拿到就变成骷髅（和用掉骸骨之书同一条路径 `CharacterData.SetSkeleton`），
  骷髅形态下几乎全免疫负面积累，另外**诅咒也不会再积累**（`AddStatus` 前缀里直接把诅咒封成 0，
  拿到时已攒的诅咒一并清零）；代价是骷髅死亡不会留下骸骨、队友没法复活你，这是骷髅状态自带的，所以没有另挂品质代价。
  回机场时会把骷髅变回人形。名额在 `HextechState.CanAcquire` 里连队友名册一起数，满员就直接从池子里跳过。
- 新增传说词条「死而复生」：获得 1 次复活机会，队友死掉后按 `技能.复活队友`（默认 `N`）
  把最近的「已死 / 完全昏迷」队友复活到使用者面前 2 米处。复用原版复活 RPC
  `Character.RPCA_ReviveAtPosition`（和骸骨之书救队友是同一条路），`applyStatus` 传 false 所以不吃复活诅咒。
- 行李箱抽奖每个抽奖位现在也可能抽到 1 ~ 100 枚商店代币（`行李箱.出代币概率`）。
- 行李箱抽奖概率调整：出海克斯 30% → **10%**，出代币 15% → **10%**。
- 行李箱抽奖的商店代币改成一次 **1 ~ 20 枚**（原来是 1 ~ 100），`行李箱.代币上限` 加了 20 的硬上限，
  配置项只能往小调。
- 右侧面板顶部新增「代币 + 商店」横条：代币数量和「按 X 打开商店」按钮单独拎出来做成一条，
  不再是角落里一行小字（之前玩家找不到商店入口）；整块面板同时上移 26 像素，但没顶到右上角那排原生 UI。
- 修复右侧面板不显示海克斯名字的问题：把名字和效果说明合并到同一个 TextMeshPro 文本里分两行渲染
  （第一行是品质图标 + 词条名，第二行是效果说明），避免原来单独的名字 Label 在运行时无法显示。
- 行李箱抽奖里负面词条的权重乘 `行李箱.负面词条权重`（默认 0.35），开箱开出代价的概率明显下降。
- 抽奖结果条除了词条名字/品质，还会显示它**当前层数的实际效果**；三选一入手时的提示也带上了效果。
- 新增白银词条「讲价高手」：海克斯商店所有商品价格减半。
  商店价格改为**查价那一刻**计算（`ShopOffer.CostFor`），所以拿到词条后立刻回到商店就是折后价。
- 新增白银词条「人猿泰山」：在藤蔓上体力消耗 -25%、藤蔓攀爬速度 +30%
  （改的是 `CharacterVineClimbing`，跟绳梯/墙壁的 `CharacterClimbing` 是两套组件，回机场一并还原）。
- 商店代币产出由「每 60 秒 1 枚」改为「每分钟 1.5 枚」（配置项 `商店.每分钟代币`，默认 1.5）。
- 移除 `icon.png`：它只在 Thunderstore 商品页当封面用，游戏里根本不读这个文件。
- 修复「背包客」扩容出来的两个格子与原有格子重叠：轮盘切片的位置全是预制体摆死的
  （基类 `UIWheel` 只处理输入、不做任何排布，`InitWheel` 也只 `SetActive`），
  原来的实现只在「切片不够、需要克隆」时把新格子沿径向推 1.55 倍 —— 既漏掉了预制体自带富余切片的情况，
  推开的距离又远小于格子本身的宽度。现在改为按原版 4 格拟合出圆心，把整圈格子按新数量重新均分，
  并放大半径保证相邻弦长不小于原版（基准位置按轮盘缓存，重复打开不会越排越远）；
  3D 外观挂点同理，放到「与模板同方向、再往外一个原版间距」的位置。

## 0.1.0

- 工程骨架、插件入口、配置项。
- 局内 3 选 1 海克斯强化系统（每人独立抽取，不共享卡池）。
- 30 条海克斯词条（9 普通 / 13 稀有 / 8 史诗），支持叠加层数。
- 6 个主动技能与能量/冷却系统（`Q` 释放、`R` 切换）。
- 中文三选一面板与 HUD（能量条、当前技能、已获得词条、操作提示）。
- 联机：词条名册通过 `PhotonView` RPC 同步，词条效果由各自客户端本地施加。
- Harmony 补丁：「坚韧之躯」减伤、「幸运旅客」开箱额外掉落（房主在原有掉落流程内插入出生点）。
